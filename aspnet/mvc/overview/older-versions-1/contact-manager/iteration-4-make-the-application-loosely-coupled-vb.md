---
uid: mvc/overview/older-versions-1/contact-manager/iteration-4-make-the-application-loosely-coupled-vb
title: 反覆運算#4 = 使應用程式鬆散耦合 (VB) |微軟文件
author: rick-anderson
description: 在第四次反覆運算中,我們利用多種軟體設計模式,使維護和修改聯繫人管理器應用程式變得更加容易。 如需...
ms.author: riande
ms.date: 02/20/2009
ms.assetid: 92c70297-4430-4e4e-919a-9c2333a8d09a
msc.legacyurl: /mvc/overview/older-versions-1/contact-manager/iteration-4-make-the-application-loosely-coupled-vb
msc.type: authoredcontent
ms.openlocfilehash: 1026e307b7e498a8b1392f2907c08cdcd05a3199
ms.sourcegitcommit: 022f79dbc1350e0c6ffaa1e7e7c6e850cdabf9af
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 04/17/2020
ms.locfileid: "81542764"
---
# <a name="iteration-4--make-the-application-loosely-coupled-vb"></a><span data-ttu-id="f75ab-104">反覆項目 #4 – 讓應用程式鬆散耦合 (VB)</span><span class="sxs-lookup"><span data-stu-id="f75ab-104">Iteration #4 – Make the application loosely coupled (VB)</span></span>

<span data-ttu-id="f75ab-105">由[微軟](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="f75ab-105">by [Microsoft](https://github.com/microsoft)</span></span>

[<span data-ttu-id="f75ab-106">下載代碼</span><span class="sxs-lookup"><span data-stu-id="f75ab-106">Download Code</span></span>](iteration-4-make-the-application-loosely-coupled-vb/_static/contactmanager_4_vb1.zip)

> <span data-ttu-id="f75ab-107">在第四次反覆運算中,我們利用多種軟體設計模式,使維護和修改聯繫人管理器應用程式變得更加容易。</span><span class="sxs-lookup"><span data-stu-id="f75ab-107">In this fourth iteration, we take advantage of several software design patterns to make it easier to maintain and modify the Contact Manager application.</span></span> <span data-ttu-id="f75ab-108">例如,我們重構應用程式以使用存儲庫模式和依賴項注入模式。</span><span class="sxs-lookup"><span data-stu-id="f75ab-108">For example, we refactor our application to use the Repository pattern and the Dependency Injection pattern.</span></span>

## <a name="building-a-contact-management-aspnet-mvc-application-vb"></a><span data-ttu-id="f75ab-109">建構 MVC 應用程式 (VB) ASP.NET連絡人管理</span><span class="sxs-lookup"><span data-stu-id="f75ab-109">Building a Contact Management ASP.NET MVC Application (VB)</span></span>

<span data-ttu-id="f75ab-110">在本系列教程中,我們從頭到尾構建整個聯繫人管理應用程式。</span><span class="sxs-lookup"><span data-stu-id="f75ab-110">In this series of tutorials, we build an entire Contact Management application from start to finish.</span></span> <span data-ttu-id="f75ab-111">透過聯絡人管理員應用程式,您可以儲存連絡人資訊 (姓名、電話號碼和電子郵件位址) 人員清單。</span><span class="sxs-lookup"><span data-stu-id="f75ab-111">The Contact Manager application enables you to store contact information - names, phone numbers and email addresses - for a list of people.</span></span>

<span data-ttu-id="f75ab-112">我們通過多次反覆運算構建應用程式。</span><span class="sxs-lookup"><span data-stu-id="f75ab-112">We build the application over multiple iterations.</span></span> <span data-ttu-id="f75ab-113">每次反覆運算時,我們都會逐步改進應用程式。</span><span class="sxs-lookup"><span data-stu-id="f75ab-113">With each iteration, we gradually improve the application.</span></span> <span data-ttu-id="f75ab-114">此多反覆運算方法的目標是使您能夠瞭解每次更改的原因。</span><span class="sxs-lookup"><span data-stu-id="f75ab-114">The goal of this multiple iteration approach is to enable you to understand the reason for each change.</span></span>

- <span data-ttu-id="f75ab-115">反覆運算#1 - 創建應用程式。</span><span class="sxs-lookup"><span data-stu-id="f75ab-115">Iteration #1 - Create the application.</span></span> <span data-ttu-id="f75ab-116">在第一次反覆運算中,我們以最簡單的方式創建聯繫人管理器。</span><span class="sxs-lookup"><span data-stu-id="f75ab-116">In the first iteration, we create the Contact Manager in the simplest way possible.</span></span> <span data-ttu-id="f75ab-117">我們添加對基本資料庫操作的支援:創建、讀取、更新和刪除 (CRUD)。</span><span class="sxs-lookup"><span data-stu-id="f75ab-117">We add support for basic database operations: Create, Read, Update, and Delete (CRUD).</span></span>

- <span data-ttu-id="f75ab-118">反覆運算#2 - 使應用程式看起來不錯。</span><span class="sxs-lookup"><span data-stu-id="f75ab-118">Iteration #2 - Make the application look nice.</span></span> <span data-ttu-id="f75ab-119">在此反覆運算中,我們通過修改預設ASP.NET MVC 檢視母版頁和級聯樣式表來改進應用程式的外觀。</span><span class="sxs-lookup"><span data-stu-id="f75ab-119">In this iteration, we improve the appearance of the application by modifying the default ASP.NET MVC view master page and cascading style sheet.</span></span>

- <span data-ttu-id="f75ab-120">反覆運算#3 - 添加表單驗證。</span><span class="sxs-lookup"><span data-stu-id="f75ab-120">Iteration #3 - Add form validation.</span></span> <span data-ttu-id="f75ab-121">在第三個反覆運算中,我們添加基本表單驗證。</span><span class="sxs-lookup"><span data-stu-id="f75ab-121">In the third iteration, we add basic form validation.</span></span> <span data-ttu-id="f75ab-122">我們防止人們在未填寫所需表單欄位的情況下提交表單。</span><span class="sxs-lookup"><span data-stu-id="f75ab-122">We prevent people from submitting a form without completing required form fields.</span></span> <span data-ttu-id="f75ab-123">我們還驗證電子郵件地址和電話號碼。</span><span class="sxs-lookup"><span data-stu-id="f75ab-123">We also validate email addresses and phone numbers.</span></span>

- <span data-ttu-id="f75ab-124">反覆運算#4 - 使應用程式鬆散耦合。</span><span class="sxs-lookup"><span data-stu-id="f75ab-124">Iteration #4 - Make the application loosely coupled.</span></span> <span data-ttu-id="f75ab-125">在第四次反覆運算中,我們利用多種軟體設計模式,使維護和修改聯繫人管理器應用程式變得更加容易。</span><span class="sxs-lookup"><span data-stu-id="f75ab-125">In this fourth iteration, we take advantage of several software design patterns to make it easier to maintain and modify the Contact Manager application.</span></span> <span data-ttu-id="f75ab-126">例如,我們重構應用程式以使用存儲庫模式和依賴項注入模式。</span><span class="sxs-lookup"><span data-stu-id="f75ab-126">For example, we refactor our application to use the Repository pattern and the Dependency Injection pattern.</span></span>

- <span data-ttu-id="f75ab-127">反覆運算#5 - 創建單元測試。</span><span class="sxs-lookup"><span data-stu-id="f75ab-127">Iteration #5 - Create unit tests.</span></span> <span data-ttu-id="f75ab-128">在第五次反覆運算中,我們通過添加單元測試使應用程式更易於維護和修改。</span><span class="sxs-lookup"><span data-stu-id="f75ab-128">In the fifth iteration, we make our application easier to maintain and modify by adding unit tests.</span></span> <span data-ttu-id="f75ab-129">我們類比數據模型類,並為控制器和驗證邏輯構建單元測試。</span><span class="sxs-lookup"><span data-stu-id="f75ab-129">We mock our data model classes and build unit tests for our controllers and validation logic.</span></span>

- <span data-ttu-id="f75ab-130">反覆運算#6 - 使用測試驅動開發。</span><span class="sxs-lookup"><span data-stu-id="f75ab-130">Iteration #6 - Use test-driven development.</span></span> <span data-ttu-id="f75ab-131">在第六次反覆運算中,我們首先編寫單元測試並針對單元測試編寫代碼,從而向應用程式添加新功能。</span><span class="sxs-lookup"><span data-stu-id="f75ab-131">In this sixth iteration, we add new functionality to our application by writing unit tests first and writing code against the unit tests.</span></span> <span data-ttu-id="f75ab-132">在此反覆運算中,我們添加聯繫人組。</span><span class="sxs-lookup"><span data-stu-id="f75ab-132">In this iteration, we add contact groups.</span></span>

- <span data-ttu-id="f75ab-133">反覆運算#7 - 添加Ajax功能。</span><span class="sxs-lookup"><span data-stu-id="f75ab-133">Iteration #7 - Add Ajax functionality.</span></span> <span data-ttu-id="f75ab-134">在第七次反覆運算中,我們通過增加對Ajax的支援來提高應用程式的回應性和性能。</span><span class="sxs-lookup"><span data-stu-id="f75ab-134">In the seventh iteration, we improve the responsiveness and performance of our application by adding support for Ajax.</span></span>

## <a name="this-iteration"></a><span data-ttu-id="f75ab-135">此反覆運算</span><span class="sxs-lookup"><span data-stu-id="f75ab-135">This Iteration</span></span>

<span data-ttu-id="f75ab-136">在 Contact Manager 應用程式的第四個反覆運算中,我們重構應用程式以使應用程式更鬆散耦合。</span><span class="sxs-lookup"><span data-stu-id="f75ab-136">In this fourth iteration of the Contact Manager application, we refactor the application to make the application more loosely coupled.</span></span> <span data-ttu-id="f75ab-137">當應用程式鬆散耦合時,可以在應用程式一個部分修改代碼,而無需修改應用程式其他部分中的代碼。</span><span class="sxs-lookup"><span data-stu-id="f75ab-137">When an application is loosely coupled, you can modify the code in one part of the application without needing to modify the code in other parts of the application.</span></span> <span data-ttu-id="f75ab-138">鬆散耦合的應用程式對更改更具彈性。</span><span class="sxs-lookup"><span data-stu-id="f75ab-138">Loosely coupled applications are more resilient to change.</span></span>

<span data-ttu-id="f75ab-139">目前,Contact Manager 應用程式使用的所有數據訪問和驗證邏輯都包含在控制器類中。</span><span class="sxs-lookup"><span data-stu-id="f75ab-139">Currently, all of the data access and validation logic used by the Contact Manager application is contained in the controller classes.</span></span> <span data-ttu-id="f75ab-140">這是個壞主意。</span><span class="sxs-lookup"><span data-stu-id="f75ab-140">This is a bad idea.</span></span> <span data-ttu-id="f75ab-141">每當需要修改應用程式的一個部分時,您都有可能將 Bug 引入應用程式的另一部分。</span><span class="sxs-lookup"><span data-stu-id="f75ab-141">Whenever you need to modify one part of your application, you risk introducing bugs into another part of your application.</span></span> <span data-ttu-id="f75ab-142">例如,如果修改驗證邏輯,則有在數據訪問或控制器邏輯中引入新 Bug 的風險。</span><span class="sxs-lookup"><span data-stu-id="f75ab-142">For example, if you modify your validation logic, you risk introducing new bugs into your data access or controller logic.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="f75ab-143">(SRP),類不應有超過一個更改的原因。</span><span class="sxs-lookup"><span data-stu-id="f75ab-143">(SRP), a class should never have more than one reason to change.</span></span> <span data-ttu-id="f75ab-144">混合控制器、驗證和資料庫邏輯嚴重違反了單一責任原則。</span><span class="sxs-lookup"><span data-stu-id="f75ab-144">Mixing controller, validation, and database logic is a massive violation of the Single Responsibility Principle.</span></span>

<span data-ttu-id="f75ab-145">可能需要修改應用程式的原因有多種。</span><span class="sxs-lookup"><span data-stu-id="f75ab-145">There are several reasons that you might need to modify your application.</span></span> <span data-ttu-id="f75ab-146">您可能需要向應用程式添加新功能,可能需要修復應用程式中的 Bug,或者可能需要修改應用程式的實現方式。</span><span class="sxs-lookup"><span data-stu-id="f75ab-146">You might need to add a new feature to your application, you might need to fix a bug in your application, or you might need to modify how a feature of your application is implemented.</span></span> <span data-ttu-id="f75ab-147">應用程式很少是靜態的。</span><span class="sxs-lookup"><span data-stu-id="f75ab-147">Applications are rarely static.</span></span> <span data-ttu-id="f75ab-148">它們往往隨著時間的推移而生長和變異。</span><span class="sxs-lookup"><span data-stu-id="f75ab-148">They tend to grow and mutate over time.</span></span>

<span data-ttu-id="f75ab-149">例如,假設您決定更改實現數據訪問層的方式。</span><span class="sxs-lookup"><span data-stu-id="f75ab-149">Imagine, for example, that you decide to change how you implement your data access layer.</span></span> <span data-ttu-id="f75ab-150">現在,連絡人管理器應用程式使用Microsoft實體框架存取資料庫。</span><span class="sxs-lookup"><span data-stu-id="f75ab-150">Right now, the Contact Manager application uses the Microsoft Entity Framework to access the database.</span></span> <span data-ttu-id="f75ab-151">但是,您可能決定遷移到新的或替代的數據訪問技術,如ADO.NET數據服務或 NHibernate。</span><span class="sxs-lookup"><span data-stu-id="f75ab-151">However, you might decide to migrate to a new or alternative data access technology such as ADO.NET Data Services or NHibernate.</span></span> <span data-ttu-id="f75ab-152">但是,由於數據訪問代碼不與驗證和控制器代碼隔離,因此如果不修改與數據訪問沒有直接關係的其他代碼,就無法修改應用程式中的數據訪問代碼。</span><span class="sxs-lookup"><span data-stu-id="f75ab-152">However, because the data access code is not isolated from the validation and controller code, there is no way to modify the data access code in your application without modifying other code that is not directly related to data access.</span></span>

<span data-ttu-id="f75ab-153">另一方面,當應用程式耦合鬆散時,您可以對應用程式的一個部分進行更改,而無需接觸應用程式的其他部分。</span><span class="sxs-lookup"><span data-stu-id="f75ab-153">When an application is loosely coupled, on the other hand, you can make changes to one part of an application without touching other parts of an application.</span></span> <span data-ttu-id="f75ab-154">例如,無需修改驗證或控制器邏輯即可切換數據訪問技術。</span><span class="sxs-lookup"><span data-stu-id="f75ab-154">For example, you can switch data access technologies without modifying your validation or controller logic.</span></span>

<span data-ttu-id="f75ab-155">在此反覆運算中,我們利用了多種軟體設計模式,使我們能夠將我們的 Contact Manager 應用程式重構為更鬆散耦合的應用程式。</span><span class="sxs-lookup"><span data-stu-id="f75ab-155">In this iteration, we take advantage of several software design patterns that enable us to refactor our Contact Manager application into a more loosely coupled application.</span></span> <span data-ttu-id="f75ab-156">完成後,聯繫人管理器不會執行以前未執行的任何操作。</span><span class="sxs-lookup"><span data-stu-id="f75ab-156">When we are done, the Contact Manager won t do anything that it didn't do before.</span></span> <span data-ttu-id="f75ab-157">但是,我們將來能夠更輕鬆地更改應用程式。</span><span class="sxs-lookup"><span data-stu-id="f75ab-157">However, we'll be able to change the application more easily in the future.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="f75ab-158">重構是重寫應用程式的過程,這樣它不會丟失任何現有功能。</span><span class="sxs-lookup"><span data-stu-id="f75ab-158">Refactoring is the process of rewriting an application in such a way that it does not lose any existing functionality.</span></span>

## <a name="using-the-repository-software-design-pattern"></a><span data-ttu-id="f75ab-159">使用儲存函式庫軟體設計模式</span><span class="sxs-lookup"><span data-stu-id="f75ab-159">Using the Repository Software Design Pattern</span></span>

<span data-ttu-id="f75ab-160">我們的第一個變化是利用稱為存儲庫模式的軟體設計模式。</span><span class="sxs-lookup"><span data-stu-id="f75ab-160">Our first change is to take advantage of a software design pattern called the Repository pattern.</span></span> <span data-ttu-id="f75ab-161">我們將使用儲存庫模式將數據訪問代碼與應用程式的其餘部分隔離開來。</span><span class="sxs-lookup"><span data-stu-id="f75ab-161">We'll use the Repository pattern to isolate our data access code from the rest of our application.</span></span>

<span data-ttu-id="f75ab-162">實現儲存庫模式需要我們完成以下兩個步驟:</span><span class="sxs-lookup"><span data-stu-id="f75ab-162">Implementing the Repository pattern requires us to complete the following two steps:</span></span>

1. <span data-ttu-id="f75ab-163">建立介面</span><span class="sxs-lookup"><span data-stu-id="f75ab-163">Create an interface</span></span>
2. <span data-ttu-id="f75ab-164">建立式介面的具體類別</span><span class="sxs-lookup"><span data-stu-id="f75ab-164">Create a concrete class that implements the interface</span></span>

<span data-ttu-id="f75ab-165">首先,我們需要創建一個介面來描述我們需要執行的所有數據訪問方法。</span><span class="sxs-lookup"><span data-stu-id="f75ab-165">First, we need to create an interface that describes all of the data access methods that we need to perform.</span></span> <span data-ttu-id="f75ab-166">IContactManager儲存庫介面包含在清單1中。</span><span class="sxs-lookup"><span data-stu-id="f75ab-166">The IContactManagerRepository interface is contained in Listing 1.</span></span> <span data-ttu-id="f75ab-167">此介面描述了五種方法:創建連絡人、刪除連絡人、編輯連絡人、獲取聯繫人和清單聯繫人()。</span><span class="sxs-lookup"><span data-stu-id="f75ab-167">This interface describes five methods: CreateContact(), DeleteContact(), EditContact(), GetContact, and ListContacts().</span></span>

<span data-ttu-id="f75ab-168">**清單 1 - 型號\IContactManagerRepository.vb**</span><span class="sxs-lookup"><span data-stu-id="f75ab-168">**Listing 1 - Models\IContactManagerRepository.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample1.vb)]

<span data-ttu-id="f75ab-169">接下來,我們需要創建一個實現 IContactManager Repository 介面的具體類。</span><span class="sxs-lookup"><span data-stu-id="f75ab-169">Next, we need to create a concrete class that implements the IContactManagerRepository interface.</span></span> <span data-ttu-id="f75ab-170">由於我們使用 Microsoft 實體框架訪問資料庫,因此我們將創建一個名為實體聯繫人管理器儲存庫的新類。</span><span class="sxs-lookup"><span data-stu-id="f75ab-170">Because we are using the Microsoft Entity Framework to access the database, we'll create a new class named EntityContactManagerRepository.</span></span> <span data-ttu-id="f75ab-171">此類包含在清單 2 中。</span><span class="sxs-lookup"><span data-stu-id="f75ab-171">This class is contained in Listing 2.</span></span>

<span data-ttu-id="f75ab-172">**清單2 - 模型_實體連絡人管理員儲存庫.vb**</span><span class="sxs-lookup"><span data-stu-id="f75ab-172">**Listing 2 - Models\EntityContactManagerRepository.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample2.vb)]

<span data-ttu-id="f75ab-173">請注意,實體聯繫人管理體儲存庫類實現了IContactManager儲存庫介面。</span><span class="sxs-lookup"><span data-stu-id="f75ab-173">Notice that the EntityContactManagerRepository class implements the IContactManagerRepository interface.</span></span> <span data-ttu-id="f75ab-174">類實現該介面描述的所有五種方法。</span><span class="sxs-lookup"><span data-stu-id="f75ab-174">The class implements all five of the methods described by that interface.</span></span>

<span data-ttu-id="f75ab-175">您可能想知道為什麼我們需要處理一個介面。</span><span class="sxs-lookup"><span data-stu-id="f75ab-175">You might wonder why we need to bother with an interface.</span></span> <span data-ttu-id="f75ab-176">為什麼我們需要同時創建介面和實現它的類?</span><span class="sxs-lookup"><span data-stu-id="f75ab-176">Why do we need to create both an interface and a class that implements it?</span></span>

<span data-ttu-id="f75ab-177">除了一個例外,我們應用程式的其餘部分將與介面而不是具體類進行交互。</span><span class="sxs-lookup"><span data-stu-id="f75ab-177">With one exception, the remainder of our application will interact with the interface and not the concrete class.</span></span> <span data-ttu-id="f75ab-178">我們將調用 IContactManagerRepository 介面公開的方法,而不是調用實體ContactManagerRepository類公開的方法。</span><span class="sxs-lookup"><span data-stu-id="f75ab-178">Instead of calling the methods exposed by the EntityContactManagerRepository class, we'll call the methods exposed by the IContactManagerRepository interface.</span></span>

<span data-ttu-id="f75ab-179">這樣,我們可以使用新類實現介面,而無需修改應用程式的其餘部分。</span><span class="sxs-lookup"><span data-stu-id="f75ab-179">That way, we can implement the interface with a new class without needing to modify the remainder of our application.</span></span> <span data-ttu-id="f75ab-180">例如,在未來某個日期,我們可能想要實現實現 IContactManagerRepository 介面的數據服務聯繫人管理器儲存庫類。</span><span class="sxs-lookup"><span data-stu-id="f75ab-180">For example, at some future date, we might want to implement an DataServicesContactManagerRepository class that implements the IContactManagerRepository interface.</span></span> <span data-ttu-id="f75ab-181">DataServicesContactManagerManagerRepository 類可能使用ADO.NET數據服務來訪問資料庫而不是Microsoft實體架構。</span><span class="sxs-lookup"><span data-stu-id="f75ab-181">The DataServicesContactManagerRepository class might use ADO.NET Data Services to access a database instead of the Microsoft Entity Framework.</span></span>

<span data-ttu-id="f75ab-182">如果我們的應用程式代碼是根據 IContactManagerRepository 介面而不是具體的 EntityContactManagerManager Repository 類程式設計的,那麼我們可以切換具體類,而無需修改我們代碼的任何其餘部分。</span><span class="sxs-lookup"><span data-stu-id="f75ab-182">If our application code is programmed against the IContactManagerRepository interface instead of the concrete EntityContactManagerRepository class then we can switch concrete classes without modifying any of the rest of our code.</span></span> <span data-ttu-id="f75ab-183">例如,我們可以從實體聯繫人管理體儲存庫類切換到 DataServicesContactManagerManagerRepository 類,而無需修改我們的數據訪問或驗證邏輯。</span><span class="sxs-lookup"><span data-stu-id="f75ab-183">For example, we can switch from the EntityContactManagerRepository class to the DataServicesContactManagerRepository class without modifying our data access or validation logic.</span></span>

<span data-ttu-id="f75ab-184">針對介面(抽象)而不是具體類進行程式設計使我們的應用程式更具彈性,從而對更改進行程式設計。</span><span class="sxs-lookup"><span data-stu-id="f75ab-184">Programming against interfaces (abstractions) instead of concrete classes makes our application more resilient to change.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="f75ab-185">您可以通過選擇功能表選項「重構」,提取介面,從 Visual Studio 中的特定類快速創建介面。</span><span class="sxs-lookup"><span data-stu-id="f75ab-185">You can quickly create an interface from a concrete class within Visual Studio by selecting the menu option Refactor, Extract Interface.</span></span> <span data-ttu-id="f75ab-186">例如,您可以先創建實體聯繫人管理體儲存庫類,然後使用提取介面自動生成 IContactManagerRepository 介面。</span><span class="sxs-lookup"><span data-stu-id="f75ab-186">For example, you can create the EntityContactManagerRepository class first and then use Extract Interface to generate the IContactManagerRepository interface automatically.</span></span>

## <a name="using-the-dependency-injection-software-design-pattern"></a><span data-ttu-id="f75ab-187">使用相依項性設計模式</span><span class="sxs-lookup"><span data-stu-id="f75ab-187">Using the Dependency Injection Software Design Pattern</span></span>

<span data-ttu-id="f75ab-188">現在,我們已經將數據訪問代碼遷移到單獨的存儲庫類,我們需要修改我們的聯繫人控制器才能使用此類。</span><span class="sxs-lookup"><span data-stu-id="f75ab-188">Now that we have migrated our data access code to a separate Repository class, we need to modify our Contact controller to use this class.</span></span> <span data-ttu-id="f75ab-189">我們將利用稱為依賴注入的軟體設計模式,在我們的控制器中使用存儲庫類。</span><span class="sxs-lookup"><span data-stu-id="f75ab-189">We will take advantage of a software design pattern called Dependency Injection to use the Repository class in our controller.</span></span>

<span data-ttu-id="f75ab-190">修改後的連絡人控制器包含在清單3中。</span><span class="sxs-lookup"><span data-stu-id="f75ab-190">The modified Contact controller is contained in Listing 3.</span></span>

<span data-ttu-id="f75ab-191">**清單3 - 控制器\聯絡控制器.vb**</span><span class="sxs-lookup"><span data-stu-id="f75ab-191">**Listing 3 - Controllers\ContactController.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample3.vb)]

<span data-ttu-id="f75ab-192">請注意,清單 3 中的聯繫人控制器有兩個構造函數。</span><span class="sxs-lookup"><span data-stu-id="f75ab-192">Notice that the Contact controller in Listing 3 has two constructors.</span></span> <span data-ttu-id="f75ab-193">第一個構造函數將IContactManagerRepository 介面的具體實例傳遞給第二個構造函數。</span><span class="sxs-lookup"><span data-stu-id="f75ab-193">The first constructor passes a concrete instance of the IContactManagerRepository interface to the second constructor.</span></span> <span data-ttu-id="f75ab-194">聯絡人控制器類別使用*建構函式相依項 。*</span><span class="sxs-lookup"><span data-stu-id="f75ab-194">The Contact controller class uses *Constructor Dependency Injection*.</span></span>

<span data-ttu-id="f75ab-195">使用實體聯繫人管理體儲存庫類的唯一位置位於第一個構造函數中。</span><span class="sxs-lookup"><span data-stu-id="f75ab-195">The one and only place that the EntityContactManagerRepository class is used is in the first constructor.</span></span> <span data-ttu-id="f75ab-196">類的其餘部分使用IContactManagerRepository介面,而不是具體的實體聯繫人管理體儲存庫類。</span><span class="sxs-lookup"><span data-stu-id="f75ab-196">The remainder of the class uses the IContactManagerRepository interface instead of the concrete EntityContactManagerRepository class.</span></span>

<span data-ttu-id="f75ab-197">這樣,將來可以輕鬆切換 IContactManagerRepository 類的實現。</span><span class="sxs-lookup"><span data-stu-id="f75ab-197">This makes it easy to switch implementations of the IContactManagerRepository class in the future.</span></span> <span data-ttu-id="f75ab-198">如果要使用 DataServicesContact 儲存庫類而不是實體聯繫人管理體儲存庫類,只需修改第一個構造函數。</span><span class="sxs-lookup"><span data-stu-id="f75ab-198">If you want to use the DataServicesContactRepository class instead of the EntityContactManagerRepository class, just modify the first constructor.</span></span>

<span data-ttu-id="f75ab-199">建構函數依賴項注入也使聯繫人控制器類非常可測試。</span><span class="sxs-lookup"><span data-stu-id="f75ab-199">Constructor Dependency injection also makes the Contact controller class very testable.</span></span> <span data-ttu-id="f75ab-200">在單元測試中,您可以通過透過 IContactManagerRepository 類的模擬實現來實例化連絡人控制器。</span><span class="sxs-lookup"><span data-stu-id="f75ab-200">In your unit tests, you can instantiate the Contact controller by passing a mock implementation of the IContactManagerRepository class.</span></span> <span data-ttu-id="f75ab-201">在下一次反覆運算中,當我們為聯繫人管理器應用程式構建單元測試時,依賴項注入的此功能對我們非常重要。</span><span class="sxs-lookup"><span data-stu-id="f75ab-201">This feature of Dependency Injection will be very important to us in the next iteration when we build unit tests for the Contact Manager application.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="f75ab-202">如果要將 Contact 控制器類與 IContactManagerRepository 介面的特定實現完全分離,則可以利用支援依賴項注入的框架,如結構映射或 Microsoft 實體框架 (MEF)。</span><span class="sxs-lookup"><span data-stu-id="f75ab-202">If you want to completely decouple the Contact controller class from a particular implementation of the IContactManagerRepository interface then you can take advantage of a framework that supports Dependency Injection such as StructureMap or the Microsoft Entity Framework (MEF).</span></span> <span data-ttu-id="f75ab-203">使用依賴項注入框架,您無需在代碼中引用具體類。</span><span class="sxs-lookup"><span data-stu-id="f75ab-203">By taking advantage of a Dependency Injection framework, you never need to refer to a concrete class in your code.</span></span>

## <a name="creating-a-service-layer"></a><span data-ttu-id="f75ab-204">建立服務層</span><span class="sxs-lookup"><span data-stu-id="f75ab-204">Creating a Service Layer</span></span>

<span data-ttu-id="f75ab-205">您可能已經注意到,我們的驗證邏輯仍然與清單 3 中修改後的控制器類中的控制器邏輯混合在一起。</span><span class="sxs-lookup"><span data-stu-id="f75ab-205">You might have noticed that our validation logic is still mixed up with our controller logic in the modified controller class in Listing 3.</span></span> <span data-ttu-id="f75ab-206">出於隔離數據訪問邏輯的一個個好主意,最好隔離我們的驗證邏輯。</span><span class="sxs-lookup"><span data-stu-id="f75ab-206">For the same reason that it is a good idea to isolate our data access logic, it is a good idea to isolate our validation logic.</span></span>

<span data-ttu-id="f75ab-207">為了解決此問題,我們可以創建一個單獨的[服務層](http://martinfowler.com/eaaCatalog/serviceLayer.html)。</span><span class="sxs-lookup"><span data-stu-id="f75ab-207">To fix this problem, we can create a separate [service layer](http://martinfowler.com/eaaCatalog/serviceLayer.html).</span></span> <span data-ttu-id="f75ab-208">服務層是一個單獨的層,我們可以插入控制器和存儲庫類之間。</span><span class="sxs-lookup"><span data-stu-id="f75ab-208">The service layer is a separate layer that we can insert between our controller and repository classes.</span></span> <span data-ttu-id="f75ab-209">服務層包含我們的業務邏輯,包括我們所有的驗證邏輯。</span><span class="sxs-lookup"><span data-stu-id="f75ab-209">The service layer contains our business logic including all of our validation logic.</span></span>

<span data-ttu-id="f75ab-210">聯繫人管理員服務包含在清單 4 中。</span><span class="sxs-lookup"><span data-stu-id="f75ab-210">The ContactManagerService is contained in Listing 4.</span></span> <span data-ttu-id="f75ab-211">它包含來自聯繫人控制器類的驗證邏輯。</span><span class="sxs-lookup"><span data-stu-id="f75ab-211">It contains the validation logic from the Contact controller class.</span></span>

<span data-ttu-id="f75ab-212">**清單4 - 型號_聯繫經理服務.vb**</span><span class="sxs-lookup"><span data-stu-id="f75ab-212">**Listing 4 - Models\ContactManagerService.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample4.vb)]

<span data-ttu-id="f75ab-213">請注意,聯繫人管理器服務的構造函數需要驗證字典。</span><span class="sxs-lookup"><span data-stu-id="f75ab-213">Notice that the constructor for the ContactManagerService requires a ValidationDictionary.</span></span> <span data-ttu-id="f75ab-214">服務層通過此驗證字典與控制器層通信。</span><span class="sxs-lookup"><span data-stu-id="f75ab-214">The service layer communicates with the controller layer through this ValidationDictionary.</span></span> <span data-ttu-id="f75ab-215">在討論修飾器模式時,我們將在下一節中詳細討論驗證詞典。</span><span class="sxs-lookup"><span data-stu-id="f75ab-215">We discuss the ValidationDictionary in detail in the following section when we discuss the Decorator pattern.</span></span>

<span data-ttu-id="f75ab-216">此外,請注意,聯繫人管理器服務實現了IContactManager服務介面。</span><span class="sxs-lookup"><span data-stu-id="f75ab-216">Notice, furthermore, that the ContactManagerService implements the IContactManagerService interface.</span></span> <span data-ttu-id="f75ab-217">您應該始終努力針對介面而不是具體類進行程式設計。</span><span class="sxs-lookup"><span data-stu-id="f75ab-217">You should always strive to program against interfaces instead of concrete classes.</span></span> <span data-ttu-id="f75ab-218">連絡人管理員應用程式中的其他類不直接與聯繫人管理器服務類交互。</span><span class="sxs-lookup"><span data-stu-id="f75ab-218">Other classes in the Contact Manager application do not interact with the ContactManagerService class directly.</span></span> <span data-ttu-id="f75ab-219">相反,除了一個例外,聯繫人管理器應用程式的其餘部分根據IContactManager服務介面進行程式設計。</span><span class="sxs-lookup"><span data-stu-id="f75ab-219">Instead, with one exception, the remainder of the Contact Manager application is programmed against the IContactManagerService interface.</span></span>

<span data-ttu-id="f75ab-220">IContactManager 服務介面包含在清單 5 中。</span><span class="sxs-lookup"><span data-stu-id="f75ab-220">The IContactManagerService interface is contained in Listing 5.</span></span>

<span data-ttu-id="f75ab-221">**清單5 - 型號_IContactManager服務.vb**</span><span class="sxs-lookup"><span data-stu-id="f75ab-221">**Listing 5 - Models\IContactManagerService.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample5.vb)]

<span data-ttu-id="f75ab-222">修改後的聯繫人控制器類包含在清單 6 中。</span><span class="sxs-lookup"><span data-stu-id="f75ab-222">The modified Contact controller class is contained in Listing 6.</span></span> <span data-ttu-id="f75ab-223">請注意,連絡人控制器不再與聯繫人管理體儲存庫進行交互。</span><span class="sxs-lookup"><span data-stu-id="f75ab-223">Notice that the Contact controller no longer interacts with the ContactManager repository.</span></span> <span data-ttu-id="f75ab-224">相反,連絡人控制器與聯繫人管理員服務進行互動。</span><span class="sxs-lookup"><span data-stu-id="f75ab-224">Instead, the Contact controller interacts with the ContactManager service.</span></span> <span data-ttu-id="f75ab-225">每個圖層都盡可能與其他圖層隔離。</span><span class="sxs-lookup"><span data-stu-id="f75ab-225">Each layer is isolated as much as possible from other layers.</span></span>

<span data-ttu-id="f75ab-226">**清單6 - 控制器\連絡控制器.vb**</span><span class="sxs-lookup"><span data-stu-id="f75ab-226">**Listing 6 - Controllers\ContactController.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample6.vb)]

<span data-ttu-id="f75ab-227">我們的應用程式不再違反單一責任原則 (SRP)。</span><span class="sxs-lookup"><span data-stu-id="f75ab-227">Our application no longer runs afoul of the Single Responsibility Principle (SRP).</span></span> <span data-ttu-id="f75ab-228">清單6中的聯繫人控制器除控制應用程式執行流外,已被剝奪了所有責任。</span><span class="sxs-lookup"><span data-stu-id="f75ab-228">The Contact controller in Listing 6 has been stripped of every responsibility other than controlling the flow of application execution.</span></span> <span data-ttu-id="f75ab-229">所有驗證邏輯都已從"連絡人"控制器中刪除,並推送到服務層。</span><span class="sxs-lookup"><span data-stu-id="f75ab-229">All the validation logic has been removed from the Contact controller and pushed into the service layer.</span></span> <span data-ttu-id="f75ab-230">所有資料庫邏輯都已推送到存儲庫層。</span><span class="sxs-lookup"><span data-stu-id="f75ab-230">All of the database logic has been pushed into the repository layer.</span></span>

## <a name="using-the-decorator-pattern"></a><span data-ttu-id="f75ab-231">使用修飾器模式</span><span class="sxs-lookup"><span data-stu-id="f75ab-231">Using the Decorator Pattern</span></span>

<span data-ttu-id="f75ab-232">我們希望能夠將服務層與控制器層完全分離。</span><span class="sxs-lookup"><span data-stu-id="f75ab-232">We want to be able to completely decouple our service layer from our controller layer.</span></span> <span data-ttu-id="f75ab-233">原則上,我們應該能夠在與控制器層分開的程式集中編譯我們的服務層,而無需添加對 MVC 應用程式的引用。</span><span class="sxs-lookup"><span data-stu-id="f75ab-233">In principle, we should be able to compile our service layer in a separate assembly from our controller layer without needing to add a reference to our MVC application.</span></span>

<span data-ttu-id="f75ab-234">但是,我們的服務層需要能夠將驗證錯誤消息傳遞回控制器層。</span><span class="sxs-lookup"><span data-stu-id="f75ab-234">However, our service layer needs to be able to pass validation error messages back to the controller layer.</span></span> <span data-ttu-id="f75ab-235">我們如何使服務層在不耦合控制器和服務層的情況下通信驗證錯誤消息?</span><span class="sxs-lookup"><span data-stu-id="f75ab-235">How can we enable the service layer to communicate validation error messages without coupling the controller and service layer?</span></span> <span data-ttu-id="f75ab-236">我們可以利用軟體設計模式,稱為[裝飾模式](http://en.wikipedia.org/wiki/Decorator_pattern)。</span><span class="sxs-lookup"><span data-stu-id="f75ab-236">We can take advantage of a software design pattern named the [Decorator pattern](http://en.wikipedia.org/wiki/Decorator_pattern).</span></span>

<span data-ttu-id="f75ab-237">控制器使用名為 ModelState 的 ModelState 字典來表示驗證錯誤。</span><span class="sxs-lookup"><span data-stu-id="f75ab-237">A controller uses a ModelStateDictionary named ModelState to represent validation errors.</span></span> <span data-ttu-id="f75ab-238">因此,您可能受誘惑將 ModelState 從控制器層傳遞到服務層。</span><span class="sxs-lookup"><span data-stu-id="f75ab-238">Therefore, you might be tempted to pass ModelState from the controller layer to the service layer.</span></span> <span data-ttu-id="f75ab-239">但是,在服務層中使用 ModelState 將使服務層依賴於ASP.NET MVC 框架的功能。</span><span class="sxs-lookup"><span data-stu-id="f75ab-239">However, using ModelState in the service layer would make your service layer dependent on a feature of the ASP.NET MVC framework.</span></span> <span data-ttu-id="f75ab-240">這將很糟糕,因為有一天,您可能希望將服務層與 WPF 應用程式一起使用,而不是使用ASP.NET MVC 應用程式。</span><span class="sxs-lookup"><span data-stu-id="f75ab-240">This would be bad because, someday, you might want to use the service layer with a WPF application instead of an ASP.NET MVC application.</span></span> <span data-ttu-id="f75ab-241">在這種情況下,您不希望引用ASP.NET MVC 框架來使用 ModelState字典類。</span><span class="sxs-lookup"><span data-stu-id="f75ab-241">In that case, you wouldn't want to reference the ASP.NET MVC framework to use the ModelStateDictionary class.</span></span>

<span data-ttu-id="f75ab-242">修飾器模式使您能夠在新類中包裝現有類,以實現介面。</span><span class="sxs-lookup"><span data-stu-id="f75ab-242">The Decorator pattern enables you to wrap an existing class in a new class in order to implement an interface.</span></span> <span data-ttu-id="f75ab-243">我們的聯繫人管理器專案包括清單 7 中包含的 ModelStateWrapper 類。</span><span class="sxs-lookup"><span data-stu-id="f75ab-243">Our Contact Manager project includes the ModelStateWrapper class contained in Listing 7.</span></span> <span data-ttu-id="f75ab-244">ModelStateWrapper 類實現清單 8 中的介面。</span><span class="sxs-lookup"><span data-stu-id="f75ab-244">The ModelStateWrapper class implements the interface in Listing 8.</span></span>

<span data-ttu-id="f75ab-245">**清單7 - 模型_驗證_模型狀態包裝.vb**</span><span class="sxs-lookup"><span data-stu-id="f75ab-245">**Listing 7 - Models\Validation\ModelStateWrapper.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample7.vb)]

<span data-ttu-id="f75ab-246">**清單8 - 模型_驗證_驗證字典.vb**</span><span class="sxs-lookup"><span data-stu-id="f75ab-246">**Listing 8 - Models\Validation\IValidationDictionary.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample8.vb)]

<span data-ttu-id="f75ab-247">如果您仔細查看清單 5,您將看到 ContactManager 服務層專用 IValidationa 字典介面。</span><span class="sxs-lookup"><span data-stu-id="f75ab-247">If you take a close look at Listing 5 then you'll see that the ContactManager service layer uses the IValidationDictionary interface exclusively.</span></span> <span data-ttu-id="f75ab-248">連絡人管理器服務不依賴於 ModelState 字典類。</span><span class="sxs-lookup"><span data-stu-id="f75ab-248">The ContactManager service is not dependent on the ModelStateDictionary class.</span></span> <span data-ttu-id="f75ab-249">當聯絡人控制器建立 ContactManager 服務時,控制器將像這樣包裝其 ModelState:</span><span class="sxs-lookup"><span data-stu-id="f75ab-249">When the Contact controller creates the ContactManager service, the controller wraps its ModelState like this:</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample9.vb)]

## <a name="summary"></a><span data-ttu-id="f75ab-250">總結</span><span class="sxs-lookup"><span data-stu-id="f75ab-250">Summary</span></span>

<span data-ttu-id="f75ab-251">在此反覆運算中,我們沒有向聯繫人管理器應用程式添加任何新功能。</span><span class="sxs-lookup"><span data-stu-id="f75ab-251">In this iteration, we did not add any new functionality to the Contact Manager application.</span></span> <span data-ttu-id="f75ab-252">此反覆運算的目標是重構聯繫人管理器應用程式,以便更易於維護和修改。</span><span class="sxs-lookup"><span data-stu-id="f75ab-252">The goal of this iteration was to refactor the Contact Manager application so that is easier to maintain and modify.</span></span>

<span data-ttu-id="f75ab-253">首先,我們實現了存儲庫軟體設計模式。</span><span class="sxs-lookup"><span data-stu-id="f75ab-253">First, we implemented the Repository software design pattern.</span></span> <span data-ttu-id="f75ab-254">我們將所有數據訪問代碼遷移到單獨的 ContactManager 儲存庫類。</span><span class="sxs-lookup"><span data-stu-id="f75ab-254">We migrated all of the data access code to a separate ContactManager repository class.</span></span>

<span data-ttu-id="f75ab-255">我們還將驗證邏輯與控制器邏輯隔離開來。</span><span class="sxs-lookup"><span data-stu-id="f75ab-255">We also isolated our validation logic from our controller logic.</span></span> <span data-ttu-id="f75ab-256">我們創建了一個單獨的服務層,其中包含我們所有的驗證代碼。</span><span class="sxs-lookup"><span data-stu-id="f75ab-256">We created a separate service layer that contains all of our validation code.</span></span> <span data-ttu-id="f75ab-257">控制器層與服務層交互,服務層與存儲庫層交互。</span><span class="sxs-lookup"><span data-stu-id="f75ab-257">The controller layer interacts with the service layer, and the service layer interacts with the repository layer.</span></span>

<span data-ttu-id="f75ab-258">創建服務層時,我們利用修飾器模式將 ModelState 與服務層隔離開來。</span><span class="sxs-lookup"><span data-stu-id="f75ab-258">When we created the service layer, we took advantage of the Decorator pattern to isolate ModelState from our service layer.</span></span> <span data-ttu-id="f75ab-259">在我們的服務層中,我們根據 I 驗證詞典介面而不是 ModelState 進行了程式設計。</span><span class="sxs-lookup"><span data-stu-id="f75ab-259">In our service layer, we programmed against the IValidationDictionary interface instead of ModelState.</span></span>

<span data-ttu-id="f75ab-260">最後,我們利用了名為依賴項注入模式的軟體設計模式。</span><span class="sxs-lookup"><span data-stu-id="f75ab-260">Finally, we took advantage of a software design pattern named the Dependency Injection pattern.</span></span> <span data-ttu-id="f75ab-261">此模式使我們能夠針對介面(抽象)而不是具體類進行程式設計。</span><span class="sxs-lookup"><span data-stu-id="f75ab-261">This pattern enables us to program against interfaces (abstractions) instead of concrete classes.</span></span> <span data-ttu-id="f75ab-262">實現依賴項注入設計模式也使我們的代碼更具可測試性。</span><span class="sxs-lookup"><span data-stu-id="f75ab-262">Implementing the Dependency Injection design pattern also makes our code more testable.</span></span> <span data-ttu-id="f75ab-263">在下一個反覆運算中,我們將單元測試添加到專案中。</span><span class="sxs-lookup"><span data-stu-id="f75ab-263">In the next iteration, we add unit tests to our project.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="f75ab-264">[前一個](iteration-3-add-form-validation-vb.md)
> [下一個](iteration-5-create-unit-tests-vb.md)</span><span class="sxs-lookup"><span data-stu-id="f75ab-264">[Previous](iteration-3-add-form-validation-vb.md)
[Next](iteration-5-create-unit-tests-vb.md)</span></span>
