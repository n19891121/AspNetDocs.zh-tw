---
uid: mvc/overview/security/xsrfcsrf-prevention-in-aspnet-mvc-and-web-pages
title: ASP.NET MVC 和 Web Pages 中的 XSRF/CSRF 防護 |Microsoft Docs
author: Rick-Anderson
description: 跨網站偽造要求 (也稱為 XSRF 或 CSRF) 是針對 web 裝載的應用程式進行攻擊，而惡意的網站可能會影響 interacti .。。
ms.author: riande
ms.date: 03/14/2013
ms.assetid: aadc5fa4-8215-4fc7-afd5-bcd2ef879728
msc.legacyurl: /mvc/overview/security/xsrfcsrf-prevention-in-aspnet-mvc-and-web-pages
msc.type: authoredcontent
ms.openlocfilehash: 7c45e342bc55ece3a6b41dcc42a0f33cd3eceffd
ms.sourcegitcommit: 4b78855427f1397df0a7be3559e04ec94a78c308
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 11/25/2020
ms.locfileid: "96151861"
---
# <a name="xsrfcsrf-prevention-in-aspnet-mvc-and-web-pages"></a><span data-ttu-id="5a060-103">ASP.NET MVC 和 ASP.NET Web Pages 中的 XSRF/CSRF 防護</span><span class="sxs-lookup"><span data-stu-id="5a060-103">XSRF/CSRF Prevention in ASP.NET MVC and Web Pages</span></span>

<span data-ttu-id="5a060-104">依 [Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="5a060-104">by [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

> <span data-ttu-id="5a060-105">跨網站要求偽造 (也稱為 XSRF 或 CSRF) 是針對 web 裝載的應用程式進行攻擊，而惡意網站可能會影響用戶端瀏覽器和該瀏覽器所信任之網站之間的互動。</span><span class="sxs-lookup"><span data-stu-id="5a060-105">Cross-site request forgery (also known as XSRF or CSRF) is an attack against web-hosted applications whereby a malicious web site can influence the interaction between a client browser and a web site trusted by that browser.</span></span> <span data-ttu-id="5a060-106">這些攻擊的原因可能是因為網頁瀏覽器會在每個網站要求時自動傳送驗證權杖。</span><span class="sxs-lookup"><span data-stu-id="5a060-106">These attacks are made possible because web browsers will send authentication tokens automatically with every request to a web site.</span></span> <span data-ttu-id="5a060-107">ASP.NET 的 Forms Authentication 票證即是驗證 Cookie 的標準範例。</span><span class="sxs-lookup"><span data-stu-id="5a060-107">The canonical example is an authentication cookie, such as ASP.NET's Forms Authentication ticket.</span></span> <span data-ttu-id="5a060-108">不過，使用任何持續性驗證機制 (例如 Windows 驗證、基本等等) 的網站，可能會以這些攻擊為目標。</span><span class="sxs-lookup"><span data-stu-id="5a060-108">However, web sites which use any persistent authentication mechanism (such as Windows Authentication, Basic, and so forth) can be targeted by these attacks.</span></span>
> 
> <span data-ttu-id="5a060-109">XSRF 攻擊與網路釣魚攻擊不同。</span><span class="sxs-lookup"><span data-stu-id="5a060-109">An XSRF attack is distinct from a phishing attack.</span></span> <span data-ttu-id="5a060-110">網路釣魚攻擊需要與受害者互動。</span><span class="sxs-lookup"><span data-stu-id="5a060-110">Phishing attacks require interaction from the victim.</span></span> <span data-ttu-id="5a060-111">在網路釣魚攻擊中，惡意網站將模仿目標網站，而犧牲者愚弄將敏感性資訊提供給攻擊者。</span><span class="sxs-lookup"><span data-stu-id="5a060-111">In a phishing attack, a malicious web site will mimic the target web site, and the victim is fooled into providing sensitive information to the attacker.</span></span> <span data-ttu-id="5a060-112">XSRF 攻擊則通常不需要與受害者互動。</span><span class="sxs-lookup"><span data-stu-id="5a060-112">In an XSRF attack, there is often no interaction necessary from the victim.</span></span> <span data-ttu-id="5a060-113">相反地，攻擊者會依賴瀏覽器自動將所有相關的 cookie 傳送至目的地網站。</span><span class="sxs-lookup"><span data-stu-id="5a060-113">Rather, the attacker is relying on the browser automatically sending all relevant cookies to the destination web site.</span></span>
> 
> <span data-ttu-id="5a060-114">如需詳細資訊，請參閱 [開放式 Web 應用程式安全性專案](https://www.owasp.org/index.php/Main_Page) (OWASP) [XSRF](https://www.owasp.org/index.php/Cross-Site_Request_Forgery_(CSRF))。</span><span class="sxs-lookup"><span data-stu-id="5a060-114">For more information, see the [Open Web Application Security Project](https://www.owasp.org/index.php/Main_Page)(OWASP) [XSRF](https://www.owasp.org/index.php/Cross-Site_Request_Forgery_(CSRF)).</span></span>

## <a name="anatomy-of-an-attack"></a><span data-ttu-id="5a060-115">攻擊的剖析</span><span class="sxs-lookup"><span data-stu-id="5a060-115">Anatomy of an attack</span></span>

<span data-ttu-id="5a060-116">若要逐步進行 XSRF 攻擊，請考慮想要執行一些線上銀行交易的使用者。</span><span class="sxs-lookup"><span data-stu-id="5a060-116">To walk through an XSRF attack, consider a user who wants to perform some online banking transactions.</span></span> <span data-ttu-id="5a060-117">此使用者會先造訪 WoodgroveBank.com 並登入，此時回應標頭會包含她的驗證 cookie：</span><span class="sxs-lookup"><span data-stu-id="5a060-117">This user first visits WoodgroveBank.com and logs in, at which point the response header will contain her authentication cookie:</span></span>

[!code-console[Main](xsrfcsrf-prevention-in-aspnet-mvc-and-web-pages/samples/sample1.cmd)]

<span data-ttu-id="5a060-118">因為驗證 cookie 是會話 cookie，所以瀏覽器會在瀏覽器進程結束時自動清除。</span><span class="sxs-lookup"><span data-stu-id="5a060-118">Because the authentication cookie is a session cookie, it will be automatically cleared by the browser when the browser process exits.</span></span> <span data-ttu-id="5a060-119">不過，在這段時間之前，瀏覽器會自動在每個要求中包含 WoodgroveBank.com 的 cookie。</span><span class="sxs-lookup"><span data-stu-id="5a060-119">However, until that time, the browser will automatically include the cookie with each request to WoodgroveBank.com.</span></span> <span data-ttu-id="5a060-120">使用者現在想要將 $1000 傳送到另一個帳戶，因此她填滿銀行網站上的表單，而瀏覽器會向伺服器提出此要求：</span><span class="sxs-lookup"><span data-stu-id="5a060-120">The user now wants to transfer $1000 to another account, so she fills out a form on the banking site, and the browser makes this request to the server:</span></span>

[!code-console[Main](xsrfcsrf-prevention-in-aspnet-mvc-and-web-pages/samples/sample2.cmd)]

<span data-ttu-id="5a060-121">因為這項作業有副作用 (它會起始) 的貨幣交易，所以銀行網站已選擇要求 HTTP POST 以起始此作業。</span><span class="sxs-lookup"><span data-stu-id="5a060-121">Because this operation has a side effect (it initiates a monetary transaction), the banking site has chosen to require an HTTP POST in order to initiate this operation.</span></span> <span data-ttu-id="5a060-122">伺服器會從要求中讀取驗證權杖、查閱目前使用者的帳戶號碼、確認有足夠的資金，然後將交易起始到目的地帳戶。</span><span class="sxs-lookup"><span data-stu-id="5a060-122">The server reads the authentication token from the request, looks up the current user's account number, verifies that sufficient funds exist, and then initiates the transaction into the destination account.</span></span>

<span data-ttu-id="5a060-123">她的線上銀行完成，使用者離開銀行網站，並造訪網路上的其他位置。</span><span class="sxs-lookup"><span data-stu-id="5a060-123">Her online banking complete, the user navigates away from the banking site and visits other locations on the web.</span></span> <span data-ttu-id="5a060-124">其中一個網站– fabrikam.com –在 iframe 中內嵌的頁面上包含下列標記 &lt; &gt; ：</span><span class="sxs-lookup"><span data-stu-id="5a060-124">One of those sites – fabrikam.com – includes the following markup on a page embedded within an &lt;iframe&gt;:</span></span>

[!code-html[Main](xsrfcsrf-prevention-in-aspnet-mvc-and-web-pages/samples/sample3.html)]

<span data-ttu-id="5a060-125">然後，瀏覽器會提出此要求：</span><span class="sxs-lookup"><span data-stu-id="5a060-125">Which then causes the browser to make this request:</span></span>

[!code-console[Main](xsrfcsrf-prevention-in-aspnet-mvc-and-web-pages/samples/sample4.cmd)]

<span data-ttu-id="5a060-126">攻擊者使用的事實是，使用者可能仍有目標網站的有效驗證權杖，而且她使用一小段 JAVAscript 來讓瀏覽器自動對目標網站發出 HTTP POST。</span><span class="sxs-lookup"><span data-stu-id="5a060-126">The attacker is exploiting the fact that the user might still have a valid authentication token for the target web site, and she is using a small snippet of Javascript to cause the browser to make an HTTP POST to the target site automatically.</span></span> <span data-ttu-id="5a060-127">如果驗證權杖仍然有效，則銀行網站將會起始將 $250 傳送到攻擊者所選擇的帳戶。</span><span class="sxs-lookup"><span data-stu-id="5a060-127">If the authentication token is still valid, the banking site will initiate a transfer of $250 into the account of the attacker's choosing.</span></span>

### <a name="ineffective-mitigations"></a><span data-ttu-id="5a060-128">不正確緩和措施</span><span class="sxs-lookup"><span data-stu-id="5a060-128">Ineffective mitigations</span></span>

<span data-ttu-id="5a060-129">值得注意的是，在上述案例中，WoodgroveBank.com 是透過 SSL 存取，並具有僅限 SSL 的驗證 cookie，不足以防止攻擊。</span><span class="sxs-lookup"><span data-stu-id="5a060-129">It is interesting to note that in the above scenario, the fact that WoodgroveBank.com was being accessed via SSL and had an SSL-only authentication cookie was insufficient to thwart the attack.</span></span> <span data-ttu-id="5a060-130">攻擊者可以在其 form 元素中指定 (HTTPs) 的 [URI 配置](http://en.wikipedia.org/wiki/URI_scheme) &lt; &gt; ，且只要這些 cookie 與預定目標的 URI 配置一致，瀏覽器就會繼續將未到期的 cookie 傳送到目標網站。</span><span class="sxs-lookup"><span data-stu-id="5a060-130">The attacker is able to specify the [URI scheme](http://en.wikipedia.org/wiki/URI_scheme) (https) in her &lt;form&gt; element, and the browser will continue to send unexpired cookies to the target site as long as those cookies are consistent with the URI scheme of the intended target.</span></span>

<span data-ttu-id="5a060-131">其中一個人可能會認為使用者不應該直接造訪不受信任的網站，因為僅造訪信任的網站可協助保持線上安全。</span><span class="sxs-lookup"><span data-stu-id="5a060-131">One could argue that the user should simply not visit untrusted sites, as visiting only trusted sites helps to remain safe online.</span></span> <span data-ttu-id="5a060-132">這項功能有一些事實，但不幸的是，這項建議並非永遠可行。</span><span class="sxs-lookup"><span data-stu-id="5a060-132">There is some truth to this, but unfortunately this advice is not always practical.</span></span> <span data-ttu-id="5a060-133">也許使用者「信任」當地新聞網站 ConsolidatedMessenger.com，並改為造訪該網站，但是該網站有 XSS 弱點，可讓攻擊者插入在 fabrikam.com 上執行的相同程式碼片段。</span><span class="sxs-lookup"><span data-stu-id="5a060-133">Perhaps the user "trusts" the local news site ConsolidatedMessenger.com and goes to visit that site instead, but that site has an XSS vulnerability which allows an attacker to inject the same snippet of code that was running on fabrikam.com.</span></span>

<span data-ttu-id="5a060-134">您可以驗證連入要求的 [推薦者標頭](http://www.w3.org/Protocols/HTTP/HTRQ_Headers.html#z14) 是否參考您的網域。</span><span class="sxs-lookup"><span data-stu-id="5a060-134">You can verify that incoming requests have a [Referer header](http://www.w3.org/Protocols/HTTP/HTRQ_Headers.html#z14) referencing your domain.</span></span> <span data-ttu-id="5a060-135">這會停止從協力廠商網域中不知情提交的要求。</span><span class="sxs-lookup"><span data-stu-id="5a060-135">This will stop requests unwittingly submitted from a third-party domain.</span></span> <span data-ttu-id="5a060-136">不過，有些人會基於隱私權原因停用其瀏覽器的推薦者標頭，而攻擊者有時可能會詐騙該標頭（如果受害者已安裝某些不安全的軟體）。</span><span class="sxs-lookup"><span data-stu-id="5a060-136">However, some people disable their browser's Referer header for privacy reasons, and attackers can sometimes spoof that header if the victim has certain insecure software installed.</span></span> <span data-ttu-id="5a060-137">確認 [推薦者標頭](http://www.w3.org/Protocols/HTTP/HTRQ_Headers.html#z14) 不會被視為防止 XSRF 攻擊的安全方法。</span><span class="sxs-lookup"><span data-stu-id="5a060-137">Verifying the [Referer header](http://www.w3.org/Protocols/HTTP/HTRQ_Headers.html#z14) is not considered a secure approach to preventing XSRF attacks.</span></span>

## <a name="web-stack-runtime-xsrf-mitigations"></a><span data-ttu-id="5a060-138">Web Stack 執行時間 XSRF 緩和措施</span><span class="sxs-lookup"><span data-stu-id="5a060-138">Web Stack Runtime XSRF mitigations</span></span>

<span data-ttu-id="5a060-139">ASP.NET Web Stack 執行時間會使用 [同步器權杖模式](https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html#synchronizer-token-pattern) 的變體來防禦 XSRF 攻擊。</span><span class="sxs-lookup"><span data-stu-id="5a060-139">The ASP.NET Web Stack Runtime uses a variant of the [synchronizer token pattern](https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html#synchronizer-token-pattern) to defend against XSRF attacks.</span></span> <span data-ttu-id="5a060-140">同步器權杖模式的一般形式是，除了驗證權杖) 之外，還會將兩個反 XSRF token 提交到伺服器 (：一個權杖作為 cookie，另一個則為表單值。</span><span class="sxs-lookup"><span data-stu-id="5a060-140">The general form of the synchronizer token pattern is that two anti-XSRF tokens are submitted to the server with each HTTP POST (In addition to the authentication token): one token as a cookie, and the other as a form value.</span></span> <span data-ttu-id="5a060-141">ASP.NET 執行時間所產生的權杖值不具決定性，也不能由攻擊者預測。</span><span class="sxs-lookup"><span data-stu-id="5a060-141">The token values generated by the ASP.NET runtime are not deterministic or predictable by an attacker.</span></span> <span data-ttu-id="5a060-142">提交權杖時，伺服器只會在兩個權杖都通過比較檢查時，才允許要求繼續進行。</span><span class="sxs-lookup"><span data-stu-id="5a060-142">When the tokens are submitted, the server will allow the request to proceed only if both tokens pass a comparison check.</span></span>

<span data-ttu-id="5a060-143">XSRF 要求驗證 *會話權杖* 會儲存為 HTTP cookie，而且目前在其承載中包含下列資訊：</span><span class="sxs-lookup"><span data-stu-id="5a060-143">The XSRF request verification *session token* is stored as an HTTP cookie and currently contains the following information in its payload:</span></span>

- <span data-ttu-id="5a060-144">由隨機128位識別碼組成的安全性權杖。</span><span class="sxs-lookup"><span data-stu-id="5a060-144">A security token, consisting of a random 128-bit identifier.</span></span>   
 <span data-ttu-id="5a060-145">下圖顯示 Internet Explorer F12 開發人員工具所顯示的 XSRF 要求驗證會話權杖： (注意這是目前的執行，甚至可能變更。 ) </span><span class="sxs-lookup"><span data-stu-id="5a060-145">The following image shows the XSRF request verification session token displayed with the Internet Explorer F12 developer tools: (Note this is the current implementation and is subject, even likely, to change.)</span></span>

![](xsrfcsrf-prevention-in-aspnet-mvc-and-web-pages/_static/image1.png)

<span data-ttu-id="5a060-146">*欄位標記* 會儲存為 `<input type="hidden" />` ，並在其承載中包含下列資訊：</span><span class="sxs-lookup"><span data-stu-id="5a060-146">The *field token* is stored as an `<input type="hidden" />` and contains the following information in its payload:</span></span>

- <span data-ttu-id="5a060-147">登入使用者的使用者名稱 (（如果已驗證) ）。</span><span class="sxs-lookup"><span data-stu-id="5a060-147">The logged-in user's username (if authenticated).</span></span>
- <span data-ttu-id="5a060-148">[IAntiForgeryAdditionalDataProvider](https://msdn.microsoft.com/library/system.web.helpers.iantiforgeryadditionaldataprovider(v=vs.111).aspx)所提供的任何其他資料。</span><span class="sxs-lookup"><span data-stu-id="5a060-148">Any additional data provided by an [IAntiForgeryAdditionalDataProvider](https://msdn.microsoft.com/library/system.web.helpers.iantiforgeryadditionaldataprovider(v=vs.111).aspx).</span></span>

<span data-ttu-id="5a060-149">XSRF token 的承載會經過加密及簽署，因此當您使用工具來檢查權杖時，就無法查看使用者名稱。</span><span class="sxs-lookup"><span data-stu-id="5a060-149">The payloads of the anti-XSRF tokens are encrypted and signed, so you can't view the username when using tools to examine the tokens.</span></span> <span data-ttu-id="5a060-150">當 web 應用程式以 ASP.NET 4.0 為目標時，密碼編譯服務會由 [MachineKey 提供。編碼](https://msdn.microsoft.com/library/system.web.security.machinekey.encode.aspx) 常式。</span><span class="sxs-lookup"><span data-stu-id="5a060-150">When the web application is targeting ASP.NET 4.0, cryptographic services are provided by the [MachineKey.Encode](https://msdn.microsoft.com/library/system.web.security.machinekey.encode.aspx) routine.</span></span> <span data-ttu-id="5a060-151">當 web 應用程式以 ASP.NET 4.5 或更高版本為目標時，MachineKey 會提供密碼編譯服務 [。保護](https://msdn.microsoft.com/library/system.web.security.machinekey.protect(v=vs.110)) 常式可提供更佳的效能、擴充性和安全性。</span><span class="sxs-lookup"><span data-stu-id="5a060-151">When the web application is targeting ASP.NET 4.5 or higher, cryptographic services are provided by the [MachineKey.Protect](https://msdn.microsoft.com/library/system.web.security.machinekey.protect(v=vs.110)) routine, which offers better performance, extensibility, and security.</span></span> <span data-ttu-id="5a060-152">如需詳細資訊，請參閱下列 blog 文章：</span><span class="sxs-lookup"><span data-stu-id="5a060-152">See the following blog posts for more details:</span></span>

- [<span data-ttu-id="5a060-153">ASP.NET 4.5，pt 中的密碼編譯增強功能</span><span class="sxs-lookup"><span data-stu-id="5a060-153">Cryptographic Improvements in ASP.NET 4.5, pt. 1</span></span>](https://blogs.msdn.com/b/webdev/archive/2012/10/22/cryptographic-improvements-in-asp-net-4-5-pt-1.aspx)
- [<span data-ttu-id="5a060-154">ASP.NET 4.5，pt 中的密碼編譯增強功能</span><span class="sxs-lookup"><span data-stu-id="5a060-154">Cryptographic Improvements in ASP.NET 4.5, pt. 2</span></span>](https://blogs.msdn.com/b/webdev/archive/2012/10/23/cryptographic-improvements-in-asp-net-4-5-pt-2.aspx)
- [<span data-ttu-id="5a060-155">ASP.NET 4.5 的密碼編譯增強功能，pt. 3</span><span class="sxs-lookup"><span data-stu-id="5a060-155">Cryptographic Improvements in ASP.NET 4.5, pt. 3</span></span>](https://blogs.msdn.com/b/webdev/archive/2012/10/24/cryptographic-improvements-in-asp-net-4-5-pt-3.aspx)

## <a name="generating-the-tokens"></a><span data-ttu-id="5a060-156">產生權杖</span><span class="sxs-lookup"><span data-stu-id="5a060-156">Generating the tokens</span></span>

<span data-ttu-id="5a060-157">若要產生反 XSRF 權杖，請 [@Html.AntiForgeryToken](https://msdn.microsoft.com/library/dd470175.aspx) 從 MVC 視圖呼叫方法，或 @AntiForgery.GetHtml 從 Razor 頁面 ( # A1。</span><span class="sxs-lookup"><span data-stu-id="5a060-157">To generate the anti-XSRF tokens, call the [@Html.AntiForgeryToken](https://msdn.microsoft.com/library/dd470175.aspx) method from an MVC view or @AntiForgery.GetHtml() from a Razor page.</span></span> <span data-ttu-id="5a060-158">執行時間會接著執行下列步驟：</span><span class="sxs-lookup"><span data-stu-id="5a060-158">The runtime will then perform the following steps:</span></span>

1. <span data-ttu-id="5a060-159">如果目前的 HTTP 要求已包含反 XSRF 會話權杖 (XSRF cookie \_ \_ RequestVerificationToken) ，則會從它解壓縮安全性權杖。</span><span class="sxs-lookup"><span data-stu-id="5a060-159">If the current HTTP request already contains an anti-XSRF session token (the anti-XSRF cookie \_\_RequestVerificationToken), the security token is extracted from it.</span></span> <span data-ttu-id="5a060-160">如果 HTTP 要求不包含反 XSRF 會話權杖，或安全性權杖的解壓縮失敗，則會產生新的隨機反 XSRF token。</span><span class="sxs-lookup"><span data-stu-id="5a060-160">If the HTTP request does not contain an anti-XSRF session token or if extraction of the security token fails, a new random anti-XSRF token will be generated.</span></span>
2. <span data-ttu-id="5a060-161">XSRF 欄位標記是使用步驟 (1) 的安全性權杖，以及目前登入使用者的身分識別產生的。</span><span class="sxs-lookup"><span data-stu-id="5a060-161">An anti-XSRF field token is generated using the security token from step (1) above and the identity of the current logged-in user.</span></span> <span data-ttu-id="5a060-162"> (如需判斷使用者身分識別的詳細資訊，請參閱下面的 **[特殊支援的案例](#_Scenarios_with_special)** 。 ) 此外，如果已設定 [IAntiForgeryAdditionalDataProvider](https://msdn.microsoft.com/library/jj158328(v=vs.111).aspx) ，執行時間會呼叫其 [GetAdditionalData](https://msdn.microsoft.com/library/system.web.helpers.iantiforgeryadditionaldataprovider.getadditionaldata(v=vs.111).aspx) 方法，並將傳回的字串包含在欄位標記中。</span><span class="sxs-lookup"><span data-stu-id="5a060-162">(For more information on determining user identity, see the **[Scenarios with special support](#_Scenarios_with_special)** section below.) Additionally, if an [IAntiForgeryAdditionalDataProvider](https://msdn.microsoft.com/library/jj158328(v=vs.111).aspx) is configured, the runtime will call its [GetAdditionalData](https://msdn.microsoft.com/library/system.web.helpers.iantiforgeryadditionaldataprovider.getadditionaldata(v=vs.111).aspx) method and include the returned string in the field token.</span></span> <span data-ttu-id="5a060-163"> (如需詳細資訊，請參閱設定 **[和](#_Configuration_and_extensibility)** 擴充性一節。 ) </span><span class="sxs-lookup"><span data-stu-id="5a060-163">(See the **[Configuration and extensibility](#_Configuration_and_extensibility)** section for more information.)</span></span>
3. <span data-ttu-id="5a060-164">如果在步驟 (1) 中產生新的防 XSRF token，則會建立新的會話權杖以包含該權杖，並將其新增至輸出 HTTP cookie 集合。</span><span class="sxs-lookup"><span data-stu-id="5a060-164">If a new anti-XSRF token was generated in step (1), a new session token will be created to contain it and will be added to the outbound HTTP cookies collection.</span></span> <span data-ttu-id="5a060-165">步驟 (2) 的欄位 token 將包裝在 `<input type="hidden" />` 元素中，而這個 HTML 標籤將是或的傳回值 `Html.AntiForgeryToken()` `AntiForgery.GetHtml()` 。</span><span class="sxs-lookup"><span data-stu-id="5a060-165">The field token from step (2) will be wrapped in an `<input type="hidden" />` element, and this HTML markup will be the return value of `Html.AntiForgeryToken()` or `AntiForgery.GetHtml()`.</span></span>

## <a name="validating-the-tokens"></a><span data-ttu-id="5a060-166">驗證權杖</span><span class="sxs-lookup"><span data-stu-id="5a060-166">Validating the tokens</span></span>

<span data-ttu-id="5a060-167">若要驗證傳入的反 XSRF token，開發人員會在其 MVC 動作或控制器上包含 [ValidateAntiForgeryToken](https://msdn.microsoft.com/library/system.web.mvc.validateantiforgerytokenattribute(VS.108).aspx) 屬性，或 `@AntiForgery.Validate()` 從她的 Razor 頁面進行呼叫。</span><span class="sxs-lookup"><span data-stu-id="5a060-167">To validate the incoming anti-XSRF tokens, the developer includes a [ValidateAntiForgeryToken](https://msdn.microsoft.com/library/system.web.mvc.validateantiforgerytokenattribute(VS.108).aspx) attribute on her MVC action or controller, or she calls `@AntiForgery.Validate()` from her Razor page.</span></span> <span data-ttu-id="5a060-168">執行時間將執行下列步驟：</span><span class="sxs-lookup"><span data-stu-id="5a060-168">The runtime will perform the following steps:</span></span>

1. <span data-ttu-id="5a060-169">系統會讀取傳入會話權杖和欄位權杖，並從每個權杖中解壓縮反 XSRF token。</span><span class="sxs-lookup"><span data-stu-id="5a060-169">The incoming session token and field token are read and the anti-XSRF token extracted from each.</span></span> <span data-ttu-id="5a060-170">XSRF 權杖在產生常式中的每個步驟 (2) 都必須相同。</span><span class="sxs-lookup"><span data-stu-id="5a060-170">The anti-XSRF tokens must be identical per step (2) in the generation routine.</span></span>
2. <span data-ttu-id="5a060-171">如果目前的使用者已通過驗證，她的使用者名稱會與儲存在欄位 token 中的使用者名稱進行比較。</span><span class="sxs-lookup"><span data-stu-id="5a060-171">If the current user is authenticated, her username is compared with the username stored in the field token.</span></span> <span data-ttu-id="5a060-172">使用者名稱必須相符。</span><span class="sxs-lookup"><span data-stu-id="5a060-172">The usernames must match.</span></span>
3. <span data-ttu-id="5a060-173">如果已設定 [IAntiForgeryAdditionalDataProvider](https://msdn.microsoft.com/library/system.web.helpers.iantiforgeryadditionaldataprovider(v=vs.111).aspx) ，執行時間會呼叫其 *ValidateAdditionalData* 方法。</span><span class="sxs-lookup"><span data-stu-id="5a060-173">If an [IAntiForgeryAdditionalDataProvider](https://msdn.microsoft.com/library/system.web.helpers.iantiforgeryadditionaldataprovider(v=vs.111).aspx) is configured, the runtime calls its *ValidateAdditionalData* method.</span></span> <span data-ttu-id="5a060-174">方法必須傳回布林值 *true*。</span><span class="sxs-lookup"><span data-stu-id="5a060-174">The method must return the Boolean value *true*.</span></span>

<span data-ttu-id="5a060-175">如果驗證成功，則允許要求繼續進行。</span><span class="sxs-lookup"><span data-stu-id="5a060-175">If validation succeeds, the request is allowed to proceed.</span></span> <span data-ttu-id="5a060-176">如果驗證失敗，架構將會擲回 *HttpAntiForgeryException*。</span><span class="sxs-lookup"><span data-stu-id="5a060-176">If validation fails, the framework will throw an *HttpAntiForgeryException*.</span></span>

## <a name="failure-conditions"></a><span data-ttu-id="5a060-177">失敗狀況</span><span class="sxs-lookup"><span data-stu-id="5a060-177">Failure conditions</span></span>

<span data-ttu-id="5a060-178">從 ASP.NET Web Stack Runtime v2 開始，驗證期間擲回的任何 *HttpAntiForgeryException* 將會包含發生錯誤的詳細資訊。</span><span class="sxs-lookup"><span data-stu-id="5a060-178">Starting with The ASP.NET Web Stack Runtime v2, any *HttpAntiForgeryException* that is thrown during validation will contain detailed information about what went wrong.</span></span> <span data-ttu-id="5a060-179">目前定義的失敗狀況如下：</span><span class="sxs-lookup"><span data-stu-id="5a060-179">The currently defined failure conditions are:</span></span>

- <span data-ttu-id="5a060-180">要求中不存在會話權杖或表單權杖。</span><span class="sxs-lookup"><span data-stu-id="5a060-180">The session token or form token is not present in the request.</span></span>
- <span data-ttu-id="5a060-181">無法讀取會話權杖或表單權杖。</span><span class="sxs-lookup"><span data-stu-id="5a060-181">The session token or form token is unreadable.</span></span> <span data-ttu-id="5a060-182">最可能的原因是伺服器陣列執行的 ASP.NET Web Stack 執行時間不相符，或伺服器陣列 &lt; 中 Web.config 的 machineKey &gt; 元素不同。</span><span class="sxs-lookup"><span data-stu-id="5a060-182">The most likely cause of this is a farm running mismatched versions of The ASP.NET Web Stack Runtime or a farm where the &lt;machineKey&gt; element in Web.config differs between machines.</span></span> <span data-ttu-id="5a060-183">您可以使用 Fiddler 之類的工具來強制執行此例外狀況，方法是使用反 XSRF token 來進行篡改。</span><span class="sxs-lookup"><span data-stu-id="5a060-183">You can use a tool such as Fiddler to force this exception by tampering with either anti-XSRF token.</span></span>
- <span data-ttu-id="5a060-184">會話權杖和欄位標記已交換。</span><span class="sxs-lookup"><span data-stu-id="5a060-184">The session token and field token were swapped.</span></span>
- <span data-ttu-id="5a060-185">會話權杖和欄位標記包含不相符的安全性權杖。</span><span class="sxs-lookup"><span data-stu-id="5a060-185">The session token and field token contain mismatched security tokens.</span></span>
- <span data-ttu-id="5a060-186">內嵌在欄位標記中的使用者名稱與目前登入使用者的使用者名稱不相符。</span><span class="sxs-lookup"><span data-stu-id="5a060-186">The username embedded within the field token does not match the current logged-in user's username.</span></span>
- <span data-ttu-id="5a060-187">*[IAntiForgeryAdditionalDataProvider. ValidateAdditionalData](https://msdn.microsoft.com/library/system.web.helpers.iantiforgeryadditionaldataprovider.validateadditionaldata(v=vs.111).aspx)* 方法傳回 *false*。</span><span class="sxs-lookup"><span data-stu-id="5a060-187">The *[IAntiForgeryAdditionalDataProvider.ValidateAdditionalData](https://msdn.microsoft.com/library/system.web.helpers.iantiforgeryadditionaldataprovider.validateadditionaldata(v=vs.111).aspx)* method returned *false*.</span></span>

<span data-ttu-id="5a060-188">防 XSRF 設施也可能在權杖產生或驗證期間執行額外檢查，而這些檢查期間的失敗可能會導致擲回例外狀況。</span><span class="sxs-lookup"><span data-stu-id="5a060-188">The anti-XSRF facilities may also perform additional checking during token generation or validation, and failures during these checks may result in exceptions being thrown.</span></span> <span data-ttu-id="5a060-189">如需詳細資訊，請參閱 [WIF/ACS/宣告式驗證](#_WIF_ACS) 和設定 **[和](#_Configuration_and_extensibility)** 擴充性章節。</span><span class="sxs-lookup"><span data-stu-id="5a060-189">See the [WIF / ACS / claims-based authentication](#_WIF_ACS) and **[Configuration and extensibility](#_Configuration_and_extensibility)** sections for more information.</span></span>

<a id="_Scenarios_with_special"></a>

## <a name="scenarios-with-special-support"></a><span data-ttu-id="5a060-190">具有特殊支援的案例</span><span class="sxs-lookup"><span data-stu-id="5a060-190">Scenarios with special support</span></span>

### <a name="anonymous-authentication"></a><span data-ttu-id="5a060-191">匿名驗證</span><span class="sxs-lookup"><span data-stu-id="5a060-191">Anonymous authentication</span></span>

<span data-ttu-id="5a060-192">反 XSRF 系統包含匿名使用者的特殊支援，其中「匿名」定義為使用者，其中 *IIdentity. IsAuthenticated* 屬性會傳回 *false*。</span><span class="sxs-lookup"><span data-stu-id="5a060-192">The anti-XSRF system contains special support for anonymous users, where "anonymous" is defined as a user where the *IIdentity.IsAuthenticated* property returns *false*.</span></span> <span data-ttu-id="5a060-193">案例包括將 XSRF 保護提供給登入頁面 (在使用者經過驗證之後) 和自訂驗證配置，其中應用程式會使用 *IIdentity* 以外的機制來識別使用者。</span><span class="sxs-lookup"><span data-stu-id="5a060-193">Scenarios include providing XSRF protection to the login page (before the user is authenticated) and custom authentication schemes where the application uses a mechanism other than *IIdentity* to identify users.</span></span>

<span data-ttu-id="5a060-194">若要支援這些案例，請記得會話和欄位標記是由安全性權杖所聯結，這是一個128位隨機產生的不透明識別碼。</span><span class="sxs-lookup"><span data-stu-id="5a060-194">To support these scenarios, recall that the session and field tokens are joined by a security token, which is a 128-bit randomly-generated opaque identifier.</span></span> <span data-ttu-id="5a060-195">此安全性權杖可用來在流覽網站時追蹤個別使用者的會話，因此它會有效地提供匿名識別碼的用途。</span><span class="sxs-lookup"><span data-stu-id="5a060-195">This security token is used to track an individual user's session as she navigates the site, so it effectively serves the purpose of an anonymous identifier.</span></span> <span data-ttu-id="5a060-196">空字串用來取代上述產生和驗證常式的使用者名稱。</span><span class="sxs-lookup"><span data-stu-id="5a060-196">An empty string is used in place of the username for the generation and validation routines described above.</span></span>

<a id="_WIF_ACS"></a>

### <a name="wif--acs--claims-based-authentication"></a><span data-ttu-id="5a060-197">WIF/ACS/宣告式驗證</span><span class="sxs-lookup"><span data-stu-id="5a060-197">WIF / ACS / claims-based authentication</span></span>

<span data-ttu-id="5a060-198">一般來說，內建于 .NET Framework 的 *IIdentity* 類別具有 *IIdentity.Name* 足夠的屬性，可唯一識別特定應用程式內的特定使用者。</span><span class="sxs-lookup"><span data-stu-id="5a060-198">Normally, the *IIdentity* classes built in to the .NET Framework have the property that *IIdentity.Name* is sufficient to uniquely identify a particular user within a particular application.</span></span> <span data-ttu-id="5a060-199">例如， *FormsIdentity.Name* 會傳回儲存在成員資格資料庫中的使用者名稱 (這對所有應用程式而言都是唯一的，因為該資料庫) 、 *WindowsIdentity.Name* 會傳回使用者的網域限定身分識別等等。</span><span class="sxs-lookup"><span data-stu-id="5a060-199">For example, *FormsIdentity.Name* returns the username stored in the membership database (which is unique for all applications depending on that database), *WindowsIdentity.Name* returns the domain-qualified identity of the user, and so on.</span></span> <span data-ttu-id="5a060-200">這些系統不僅提供驗證，它們也會 *識別* 應用程式的使用者。</span><span class="sxs-lookup"><span data-stu-id="5a060-200">These systems provide not only authentication; they also *identify* users to an application.</span></span>

<span data-ttu-id="5a060-201">另一方面，以宣告為基礎的驗證不一定需要識別特定的使用者。</span><span class="sxs-lookup"><span data-stu-id="5a060-201">Claims-based authentication, on the other hand, does not necessarily require identifying a particular user.</span></span> <span data-ttu-id="5a060-202">相反地， *ClaimsPrincipal* 和 *ClaimsIdentity* 類型會與一組宣告實例相關聯，其中個別宣告可能是 "是 18 + 年的年齡" 或 "是系統管理員 *" 的任何* 其他宣告。</span><span class="sxs-lookup"><span data-stu-id="5a060-202">Instead, the *ClaimsPrincipal* and *ClaimsIdentity* types are associated with a set of *Claim* instances, where the individual claims might be "is 18+ years of age" or "is an administrator" to anything else.</span></span> <span data-ttu-id="5a060-203">因為不一定會識別使用者，所以執行時間不能使用 *ClaimsIdentity.Name* 屬性做為此特定使用者的唯一識別碼。</span><span class="sxs-lookup"><span data-stu-id="5a060-203">Since the user hasn't necessarily been identified, the runtime cannot use the *ClaimsIdentity.Name* property as a unique identifier for this particular user.</span></span> <span data-ttu-id="5a060-204">小組已經看過真實世界的範例，其中的 *ClaimsIdentity.Name* 會傳回 *null*、傳回易記的 (顯示) 名稱，或傳回不適合做為使用者唯一識別碼的字串。</span><span class="sxs-lookup"><span data-stu-id="5a060-204">The team has seen real-world examples where *ClaimsIdentity.Name* returns *null*, returns a friendly (display) name, or otherwise returns a string that isn't appropriate for use as a unique identifier for the user.</span></span>

<span data-ttu-id="5a060-205">許多使用宣告式驗證的部署都是使用 [Azure 存取控制服務](https://msdn.microsoft.com/library/windowsazure/gg429786.aspx) (ACS) 。</span><span class="sxs-lookup"><span data-stu-id="5a060-205">Many of deployments which use claims-based authentication are using [Azure Access Control Service](https://msdn.microsoft.com/library/windowsazure/gg429786.aspx) (ACS) in particular.</span></span> <span data-ttu-id="5a060-206">ACS 可讓開發人員設定個別的身分 *識別提供 (者* ，例如 ADFS、Microsoft 帳戶提供者、OpenID 提供者（例如 yahoo！等） ) ，以及身分識別提供者傳回 *名稱識別碼*。</span><span class="sxs-lookup"><span data-stu-id="5a060-206">ACS allows the developer to configure individual *identity providers* (such as ADFS, the Microsoft Account provider, OpenID providers like Yahoo!, etc.), and the identity providers return *name identifiers*.</span></span> <span data-ttu-id="5a060-207">這些名稱識別碼可能包含個人識別資訊 (PII) 例如電子郵件地址，也可以像私人個人識別碼 (PPID) 一樣匿名。</span><span class="sxs-lookup"><span data-stu-id="5a060-207">These name identifiers may contain Personally Identifiable Information (PII) like an email address, or they could be anonymized like a Private Personal Identifier (PPID).</span></span> <span data-ttu-id="5a060-208">無論是哪一種，元組 (身分識別提供者、名稱識別碼) 足以作為特定使用者在流覽網站時的適當追蹤權杖，所以 ASP.NET Web Stack 執行時間可以在產生並驗證反 XSRF 欄位 token 時，使用元組取代使用者名稱。</span><span class="sxs-lookup"><span data-stu-id="5a060-208">Regardless, the tuple (identity provider, name identifier) sufficiently serves as an appropriate tracking token for a particular user while she is browsing the site, so the ASP.NET Web Stack Runtime can use the tuple in place of the username when generating and validating anti-XSRF field tokens.</span></span> <span data-ttu-id="5a060-209">身分識別提供者和名稱識別碼的特定 Uri 如下：</span><span class="sxs-lookup"><span data-stu-id="5a060-209">The particular URIs for the identity provider and the name identifier are :</span></span>

- `https://schemas.microsoft.com/accesscontrolservice/2010/07/claims/identityprovider`
- `http://schemas.xmlsoap.org/ws/2005/05/identity/claims/nameidentifier`

<span data-ttu-id="5a060-210"> (如需詳細資訊，請參閱此 [ACS doc 頁面](https://msdn.microsoft.com/library/windowsazure/gg185971.aspx) 。 ) </span><span class="sxs-lookup"><span data-stu-id="5a060-210">(see this [ACS doc page](https://msdn.microsoft.com/library/windowsazure/gg185971.aspx) for more info.)</span></span>

<span data-ttu-id="5a060-211">產生或驗證權杖時，ASP.NET Web Stack 執行時間會在執行時間嘗試系結至類型：</span><span class="sxs-lookup"><span data-stu-id="5a060-211">When generating or validating a token, the ASP.NET Web Stack Runtime will at runtime try binding to the types:</span></span>

- <span data-ttu-id="5a060-212">`Microsoft.IdentityModel.Claims.IClaimsIdentity, Microsoft.IdentityModel, Version=3.5.0.0, Culture=neutral, PublicKeyToken=31bf3856ad364e35` WIF SDK 的 (。 ) </span><span class="sxs-lookup"><span data-stu-id="5a060-212">`Microsoft.IdentityModel.Claims.IClaimsIdentity, Microsoft.IdentityModel, Version=3.5.0.0, Culture=neutral, PublicKeyToken=31bf3856ad364e35` (For the WIF SDK.)</span></span>
- <span data-ttu-id="5a060-213">`System.Security.Claims.ClaimsIdentity` 適用于 .NET 4.5) 的 (。</span><span class="sxs-lookup"><span data-stu-id="5a060-213">`System.Security.Claims.ClaimsIdentity` (For .NET 4.5).</span></span>

<span data-ttu-id="5a060-214">如果這些類型存在，而且目前使用者的 *IIIIdentity* 會執行其中一種類型，則反 XSRF 設備會使用 (識別提供者、名稱識別碼) 元組來取代在產生和驗證權杖時的使用者名稱。</span><span class="sxs-lookup"><span data-stu-id="5a060-214">If these types exist, and if the current user's *IIIIdentity* implements or subclasses one of these types, the anti-XSRF facility will use the (identity provider, name identifier) tuple in place of the username when generating and validating the tokens.</span></span> <span data-ttu-id="5a060-215">如果沒有這樣的元組，要求將會失敗，並出現錯誤，說明開發人員如何設定防 XSRF 系統，以瞭解使用中的特定宣告式驗證機制。</span><span class="sxs-lookup"><span data-stu-id="5a060-215">If no such tuple is present, the request will fail with an error describing to the developer how to configure the anti-XSRF system to understand the particular claims-based authentication mechanism in use.</span></span> <span data-ttu-id="5a060-216">如需詳細資訊，請參閱設定 **[和](#_Configuration_and_extensibility)** 擴充性一節。</span><span class="sxs-lookup"><span data-stu-id="5a060-216">See the **[Configuration and extensibility](#_Configuration_and_extensibility)** section for more information.</span></span>

### <a name="oauth--openid-authentication"></a><span data-ttu-id="5a060-217">OAuth/OpenID 驗證</span><span class="sxs-lookup"><span data-stu-id="5a060-217">OAuth / OpenID authentication</span></span>

<span data-ttu-id="5a060-218">最後，反 XSRF 設備對使用 OAuth 或 OpenID 驗證的應用程式具有特殊支援。</span><span class="sxs-lookup"><span data-stu-id="5a060-218">Finally, the anti-XSRF facility has special support for applications which use OAuth or OpenID authentication.</span></span> <span data-ttu-id="5a060-219">這項支援是以啟發學習法為基礎：如果目前 *IIdentity.Name* 的開頭是 HTTP://或 HTTPs://，則會使用序數比較子來進行使用者名稱比較，而不是使用預設 OrdinalIgnoreCase 比較子。</span><span class="sxs-lookup"><span data-stu-id="5a060-219">This support is heuristic-based: if the current *IIdentity.Name* begins with http:// or https://, then username comparisons will be done using an Ordinal comparer rather than the default OrdinalIgnoreCase comparer.</span></span>

<a id="_Configuration_and_extensibility"></a>

## <a name="configuration-and-extensibility"></a><span data-ttu-id="5a060-220">設定和擴充性</span><span class="sxs-lookup"><span data-stu-id="5a060-220">Configuration and extensibility</span></span>

<span data-ttu-id="5a060-221">有時候，開發人員可能想要對反 XSRF 產生和驗證行為進行更嚴密的控制。</span><span class="sxs-lookup"><span data-stu-id="5a060-221">Occasionally, developers may want tighter control over the anti-XSRF generation and validation behaviors.</span></span> <span data-ttu-id="5a060-222">例如，MVC 和網頁協助程式的預設行為（自動將 HTTP cookie 新增至回應）是不必要的，而開發人員可能會想要在其他地方保存權杖。</span><span class="sxs-lookup"><span data-stu-id="5a060-222">For example, perhaps the MVC and Web Pages helpers' default behavior of automatically adding HTTP cookies to the response is undesirable, and the developer may wish to persist the tokens elsewhere.</span></span> <span data-ttu-id="5a060-223">有兩個 Api 可協助您：</span><span class="sxs-lookup"><span data-stu-id="5a060-223">There exist two APIs to assist with this:</span></span>

`AntiForgery.GetTokens(string oldCookieToken, out string newCookieToken, out string formToken);`  
`AntiForgery.Validate(string cookieToken, string formToken);`

<span data-ttu-id="5a060-224">*GetTokens* 方法會以現有的 XSRF 要求驗證會話權杖作為輸入 (這可能是 null) ，並產生新的 XSRF 要求驗證會話權杖和欄位 token 作為輸出。</span><span class="sxs-lookup"><span data-stu-id="5a060-224">The *GetTokens* method takes as input an existing XSRF request verification session token (which may be null) and produces as output a new XSRF request verification session token and field token.</span></span> <span data-ttu-id="5a060-225">權杖是不透明的字串，不含裝飾;實例的 *formToken* 值不會包裝在 &lt; 輸入標記中 &gt; 。</span><span class="sxs-lookup"><span data-stu-id="5a060-225">The tokens are simply opaque strings with no decoration; the *formToken* value will for instance not be wrapped in an &lt;input&gt; tag.</span></span> <span data-ttu-id="5a060-226">*NewCookieToken* 值可能是 null;如果發生這種情況， *oldCookieToken* 值仍然有效，且不需要設定任何新的回應 cookie。</span><span class="sxs-lookup"><span data-stu-id="5a060-226">The *newCookieToken* value may be null; if this occurs, then the *oldCookieToken* value is still valid and no new response cookie need be set.</span></span> <span data-ttu-id="5a060-227">*GetTokens* 的呼叫端負責保存任何必要的回應 cookie，或產生任何必要的標記;*GetTokens* 方法本身不會將回應變更為副作用。</span><span class="sxs-lookup"><span data-stu-id="5a060-227">The caller of *GetTokens* is responsible for persisting any necessary response cookies or generating any necessary markup; the *GetTokens* method itself will not alter the response as a side effect.</span></span> <span data-ttu-id="5a060-228">*Validate* 方法會接受傳入的會話和欄位權杖，並對其執行上述驗證邏輯。</span><span class="sxs-lookup"><span data-stu-id="5a060-228">The *Validate* method takes the incoming session and field tokens and runs the aforementioned validation logic over them.</span></span>

### <a name="antiforgeryconfig"></a><span data-ttu-id="5a060-229">AntiForgeryConfig</span><span class="sxs-lookup"><span data-stu-id="5a060-229">AntiForgeryConfig</span></span>

<span data-ttu-id="5a060-230">開發人員可能會從應用程式啟動設定防 XSRF 系統 \_ 。</span><span class="sxs-lookup"><span data-stu-id="5a060-230">The developer may configure the anti-XSRF system from Application\_Start.</span></span> <span data-ttu-id="5a060-231">設定是以程式設計方式進行。</span><span class="sxs-lookup"><span data-stu-id="5a060-231">Configuration is programmatic.</span></span> <span data-ttu-id="5a060-232">靜態 *AntiForgeryConfig* 類型的屬性如下所述。</span><span class="sxs-lookup"><span data-stu-id="5a060-232">The properties of the static *AntiForgeryConfig* type are described below.</span></span> <span data-ttu-id="5a060-233">大部分使用宣告的使用者都想要設定 UniqueClaimTypeIdentifier 屬性。</span><span class="sxs-lookup"><span data-stu-id="5a060-233">Most users using claims will want to set the UniqueClaimTypeIdentifier property.</span></span>

| <span data-ttu-id="5a060-234">**屬性**</span><span class="sxs-lookup"><span data-stu-id="5a060-234">**Property**</span></span> | <span data-ttu-id="5a060-235">**說明**</span><span class="sxs-lookup"><span data-stu-id="5a060-235">**Description**</span></span> |
| --- | --- |
| <span data-ttu-id="5a060-236">**AdditionalDataProvider**</span><span class="sxs-lookup"><span data-stu-id="5a060-236">**AdditionalDataProvider**</span></span> | <span data-ttu-id="5a060-237">在權杖產生期間提供額外資料的 [IAntiForgeryAdditionalDataProvider](https://msdn.microsoft.com/library/system.web.helpers.iantiforgeryadditionaldataprovider(v=vs.111).aspx) ，並在權杖驗證期間取用其他資料。</span><span class="sxs-lookup"><span data-stu-id="5a060-237">An [IAntiForgeryAdditionalDataProvider](https://msdn.microsoft.com/library/system.web.helpers.iantiforgeryadditionaldataprovider(v=vs.111).aspx) that provides additional data during token generation and consumes additional data during token validation.</span></span> <span data-ttu-id="5a060-238">預設值為 *null*。</span><span class="sxs-lookup"><span data-stu-id="5a060-238">The default value is *null*.</span></span> <span data-ttu-id="5a060-239">如需詳細資訊，請參閱 [IAntiForgeryAdditionalDataProvider](https://msdn.microsoft.com/library/system.web.helpers.iantiforgeryadditionaldataprovider(v=vs.111).aspx) 一節。</span><span class="sxs-lookup"><span data-stu-id="5a060-239">For more information, see the [IAntiForgeryAdditionalDataProvider](https://msdn.microsoft.com/library/system.web.helpers.iantiforgeryadditionaldataprovider(v=vs.111).aspx) section.</span></span> |
| <span data-ttu-id="5a060-240">**CookieName**</span><span class="sxs-lookup"><span data-stu-id="5a060-240">**CookieName**</span></span> | <span data-ttu-id="5a060-241">字串，提供用來儲存反 XSRF 會話權杖之 HTTP cookie 的名稱。</span><span class="sxs-lookup"><span data-stu-id="5a060-241">A string that provides the name of the HTTP cookie that is used to store the anti-XSRF session token.</span></span> <span data-ttu-id="5a060-242">如果未設定此值，將會根據應用程式部署的虛擬路徑自動產生名稱。</span><span class="sxs-lookup"><span data-stu-id="5a060-242">If this value is not set, a name will be automatically generated based on the application's deployed virtual path.</span></span> <span data-ttu-id="5a060-243">預設值為 *null*。</span><span class="sxs-lookup"><span data-stu-id="5a060-243">The default value is *null*.</span></span> |
| <span data-ttu-id="5a060-244">**RequireSsl**</span><span class="sxs-lookup"><span data-stu-id="5a060-244">**RequireSsl**</span></span> | <span data-ttu-id="5a060-245">布林值，指定是否需要透過 SSL 安全通道來提交反 XSRF token。</span><span class="sxs-lookup"><span data-stu-id="5a060-245">A Boolean that dictates whether the anti-XSRF tokens are required to be submitted over an SSL-secured channel.</span></span> <span data-ttu-id="5a060-246">如果此值為 *true*，任何自動產生的 cookie 都會設定「安全」旗標，而且如果是從不是透過 SSL 提交的要求內呼叫，則會擲回反 XSRF api。</span><span class="sxs-lookup"><span data-stu-id="5a060-246">If this value is *true*, any automatically-generated cookies will have the "secure" flag set, and the anti-XSRF APIs will throw if called from within a request that is not submitted via SSL.</span></span> <span data-ttu-id="5a060-247">預設值為 *false*。</span><span class="sxs-lookup"><span data-stu-id="5a060-247">The default value is *false*.</span></span> |
| <span data-ttu-id="5a060-248">**SuppressIdentityHeuristicChecks**</span><span class="sxs-lookup"><span data-stu-id="5a060-248">**SuppressIdentityHeuristicChecks**</span></span> | <span data-ttu-id="5a060-249">布林值，指定反 XSRF 系統是否應該停用其對宣告型身分識別的支援。</span><span class="sxs-lookup"><span data-stu-id="5a060-249">A Boolean that dictates whether the anti-XSRF system should deactivate its support for claims-based identities.</span></span> <span data-ttu-id="5a060-250">如果這個值為 *true*，系統會假設 *IIdentity.Name* 適用于做為唯一的每個使用者識別碼，而且不會嘗試如 [WIF/ACS/宣告式驗證](#_WIF_ACS)一節中所述的特殊案例 *IClaimsIdentity* 或 *ClClaimsIdentity* 。</span><span class="sxs-lookup"><span data-stu-id="5a060-250">If this value is *true*, the system will assume that *IIdentity.Name* is appropriate for use as a unique per-user identifier and will not try to special-case *IClaimsIdentity* or *ClClaimsIdentity* as described in the [WIF / ACS / claims-based authentication](#_WIF_ACS) section.</span></span> <span data-ttu-id="5a060-251">預設值為 `false`。</span><span class="sxs-lookup"><span data-stu-id="5a060-251">The default value is `false`.</span></span> |
| <span data-ttu-id="5a060-252">**UniqueClaimTypeIdentifier**</span><span class="sxs-lookup"><span data-stu-id="5a060-252">**UniqueClaimTypeIdentifier**</span></span> | <span data-ttu-id="5a060-253">字串，指出適合用來做為唯一每個使用者識別碼的宣告類型。</span><span class="sxs-lookup"><span data-stu-id="5a060-253">A string that indicates which claim type is appropriate for use as a unique per-user identifier.</span></span> <span data-ttu-id="5a060-254">如果已設定此值，且目前的 *IIdentity* 是以宣告為基礎，系統將會嘗試解壓縮 *UniqueClaimTypeIdentifier* 所指定之類型的宣告，而且在產生欄位標記時，會使用對應的值來取代使用者的使用者名稱。</span><span class="sxs-lookup"><span data-stu-id="5a060-254">If this value is set and the current *IIdentity* is claims-based, the system will attempt to extract a claim of the type specified by *UniqueClaimTypeIdentifier*, and the corresponding value will be used in place of the user's username when generating the field token.</span></span> <span data-ttu-id="5a060-255">如果找不到宣告類型，系統將會導致要求失敗。</span><span class="sxs-lookup"><span data-stu-id="5a060-255">If the claim type is not found, the system will fail the request.</span></span> <span data-ttu-id="5a060-256">預設值為 *null*，表示系統應使用 (識別提供者、名稱識別碼) 元組，如先前所述，取代使用者的使用者名稱。</span><span class="sxs-lookup"><span data-stu-id="5a060-256">The default value is *null*, which indicates that the system should use the (identity provider, name identifier) tuple as previously described in place of the user's username.</span></span> |

<a id="_IAntiForgeryAdditionalDataProvider"></a>

### <a name="iantiforgeryadditionaldataprovider"></a><span data-ttu-id="5a060-257">IAntiForgeryAdditionalDataProvider</span><span class="sxs-lookup"><span data-stu-id="5a060-257">IAntiForgeryAdditionalDataProvider</span></span>

<span data-ttu-id="5a060-258">*[IAntiForgeryAdditionalDataProvider](https://msdn.microsoft.com/library/system.web.helpers.iantiforgeryadditionaldataprovider(v=vs.111).aspx)* 型別可讓開發人員藉由在每個權杖中來回往返額外資料，來擴充反 XSRF 系統的行為。</span><span class="sxs-lookup"><span data-stu-id="5a060-258">The *[IAntiForgeryAdditionalDataProvider](https://msdn.microsoft.com/library/system.web.helpers.iantiforgeryadditionaldataprovider(v=vs.111).aspx)* type allows developers to extend the behavior of the anti-XSRF system by round-tripping additional data in each token.</span></span> <span data-ttu-id="5a060-259">每次產生欄位 token 時都會呼叫 *GetAdditionalData* 方法，並將傳回值內嵌在產生的權杖內。</span><span class="sxs-lookup"><span data-stu-id="5a060-259">The *GetAdditionalData* method is called each time a field token is generated, and the return value is embedded within the generated token.</span></span> <span data-ttu-id="5a060-260">實施者可能會從這個方法傳回時間戳記、nonce 或任何其他值。</span><span class="sxs-lookup"><span data-stu-id="5a060-260">An implementer could return a timestamp, a nonce, or any other value she wishes from this method.</span></span>

<span data-ttu-id="5a060-261">同樣地， *ValidateAdditionalData* 方法會在每次驗證欄位 token 時呼叫，並將內嵌在權杖內的「其他資料」字串傳遞給方法。</span><span class="sxs-lookup"><span data-stu-id="5a060-261">Similarly, the *ValidateAdditionalData* method is called each time a field token is validated, and the "additional data" string that was embedded within the token is passed to the method.</span></span> <span data-ttu-id="5a060-262">驗證常式可以藉由檢查目前的時間與建立權杖時所儲存的時間) 、nonce 檢查常式或任何其他所需的邏輯，來執行超時 (。</span><span class="sxs-lookup"><span data-stu-id="5a060-262">The validation routine could implement a timeout (by checking the current time against the time that was stored when the token was created), a nonce checking routine, or any other desired logic.</span></span>

## <a name="design-decisions-and-security-considerations"></a><span data-ttu-id="5a060-263">設計決策和安全性考慮</span><span class="sxs-lookup"><span data-stu-id="5a060-263">Design decisions and security considerations</span></span>

<span data-ttu-id="5a060-264">只有在嘗試防止匿名/未經驗證的使用者遭受 XSRF 攻擊時，才需要在技術上連結會話和欄位權杖的安全性權杖。</span><span class="sxs-lookup"><span data-stu-id="5a060-264">The security token that links the session and field tokens is technically only necessary when trying to protect anonymous / unauthenticated users against XSRF attacks.</span></span> <span data-ttu-id="5a060-265">當使用者經過驗證時，驗證權杖本身 (應以 cookie 形式提交) 可作為同步器權杖配對的一半。</span><span class="sxs-lookup"><span data-stu-id="5a060-265">When the user is authenticated, the authentication token itself (presumably submitted in the form of a cookie) could be used as one half of a synchronizer token pair.</span></span> <span data-ttu-id="5a060-266">不過，有一個有效的案例可保護未經過驗證的使用者所叫用的登入頁面，而反 XSRF 邏輯則是透過永遠產生和驗證安全性權杖（即使是已驗證的使用者）來簡化。</span><span class="sxs-lookup"><span data-stu-id="5a060-266">However, there are valid scenarios for protecting login pages hit by unauthenticated users, and the anti-XSRF logic was made simpler by always generating and validating the security token, even for authenticated users.</span></span> <span data-ttu-id="5a060-267">如果攻擊者入侵了欄位權杖，也可以提供一些額外的保護，因為設定或猜測會話權杖會是攻擊者克服的另一個障礙。</span><span class="sxs-lookup"><span data-stu-id="5a060-267">It also does provide some additional protection in the event that a field token is ever compromised by an attacker, as setting or guessing the session token would be another hurdle for the attacker to overcome.</span></span>

<span data-ttu-id="5a060-268">在單一網域中裝載多個應用程式時，開發人員應謹慎使用。</span><span class="sxs-lookup"><span data-stu-id="5a060-268">Developers should use caution when multiple applications are hosted in a single domain.</span></span> <span data-ttu-id="5a060-269">例如，雖然 *example1.cloudapp.net* 和 *example2.cloudapp.net* 是不同的主機，但 *\* cloudapp.net* 網域下的所有主機之間都有隱含的信任關係。</span><span class="sxs-lookup"><span data-stu-id="5a060-269">For example, even though *example1.cloudapp.net* and *example2.cloudapp.net* are different hosts, there is an implicit trust relationship between all hosts under the *\*.cloudapp.net* domain.</span></span> <span data-ttu-id="5a060-270">此隱含信任關聯性 [可讓可能不受信任的主機影響彼此的 cookie](http://stackoverflow.com/questions/9636857/how-can-asp-net-or-asp-net-mvc-be-protected-from-related-domain-cookie-attacks) (管理 AJAX 要求的相同原始原則不一定適用于) 的 HTTP cookie。</span><span class="sxs-lookup"><span data-stu-id="5a060-270">This implicit trust relationship [allows potentially untrusted hosts to affect each other's cookies](http://stackoverflow.com/questions/9636857/how-can-asp-net-or-asp-net-mvc-be-protected-from-related-domain-cookie-attacks) (the same-origin policies that govern AJAX requests do not necessarily apply to HTTP cookies).</span></span> <span data-ttu-id="5a060-271">ASP.NET Web Stack 執行時間提供了一些風險降低，因為使用者名稱已內嵌到欄位 token 中，所以即使惡意子域可以覆寫會話權杖，也無法為使用者產生有效的欄位 token。</span><span class="sxs-lookup"><span data-stu-id="5a060-271">The ASP.NET Web Stack Runtime provides some mitigation in that the username is embedded into the field token, so even if a malicious subdomain is able to overwrite a session token it will be unable to generate a valid field token for the user.</span></span> <span data-ttu-id="5a060-272">不過，在這種環境中裝載時，內建的防 XSRF 常式仍無法防禦會話劫持或登入 XSRF。</span><span class="sxs-lookup"><span data-stu-id="5a060-272">However, when hosted in such an environment the built-in anti-XSRF routines still cannot defend against session hijacking or login XSRF.</span></span>

<span data-ttu-id="5a060-273">反 XSRF 常式目前不會防禦 [點擊劫持](https://www.owasp.org/index.php/Clickjacking)。</span><span class="sxs-lookup"><span data-stu-id="5a060-273">The anti-XSRF routines currently do not defend against [clickjacking](https://www.owasp.org/index.php/Clickjacking).</span></span> <span data-ttu-id="5a060-274">想要針對點擊劫持防護的應用程式，可以藉由傳送 X 框架選項： SAMEORIGIN 標頭與每個回應來輕鬆執行此動作。</span><span class="sxs-lookup"><span data-stu-id="5a060-274">Applications that wish to defend themselves against clickjacking may easily do so by sending an X-Frame-Options: SAMEORIGIN header with each response.</span></span> <span data-ttu-id="5a060-275">所有最新的瀏覽器都支援此標頭。</span><span class="sxs-lookup"><span data-stu-id="5a060-275">This header is supported by all recent browsers.</span></span> <span data-ttu-id="5a060-276">如需詳細資訊，請參閱 [IE blog](https://blogs.msdn.com/b/ieinternals/archive/2010/03/30/combating-clickjacking-with-x-frame-options.aspx)、 [SDL blog](https://blogs.msdn.com/b/sdl/archive/2009/02/05/clickjacking-defense-in-ie8.aspx)和 [OWASP](https://www.owasp.org/index.php/Clickjacking)。</span><span class="sxs-lookup"><span data-stu-id="5a060-276">For more information, see the [IE blog](https://blogs.msdn.com/b/ieinternals/archive/2010/03/30/combating-clickjacking-with-x-frame-options.aspx), the [SDL blog](https://blogs.msdn.com/b/sdl/archive/2009/02/05/clickjacking-defense-in-ie8.aspx), and [OWASP](https://www.owasp.org/index.php/Clickjacking).</span></span> <span data-ttu-id="5a060-277">在未來的版本中，ASP.NET Web Stack 執行時間可能會使 MVC 和網頁反 XSRF 協助程式自動設定此標頭，讓應用程式能夠自動受到這項攻擊的保護。</span><span class="sxs-lookup"><span data-stu-id="5a060-277">The ASP.NET Web Stack Runtime may in some future release make the MVC and Web Pages anti-XSRF helpers automatically set this header so that applications are automatically protected against this attack.</span></span>

<span data-ttu-id="5a060-278">Web 開發人員應繼續確保其網站不容易遭受 XSS 攻擊。</span><span class="sxs-lookup"><span data-stu-id="5a060-278">Web developers should continue to ensure that their site is not vulnerable to XSS attacks.</span></span> <span data-ttu-id="5a060-279">XSS 攻擊的功能非常強大，而成功的惡意探索也會中斷 ASP.NET Web Stack 執行時間防禦以防範 XSRF 攻擊。</span><span class="sxs-lookup"><span data-stu-id="5a060-279">XSS attacks are very powerful, and a successful exploit would also break the ASP.NET Web Stack Runtime defenses against XSRF attacks.</span></span>

## <a name="acknowledgment"></a><span data-ttu-id="5a060-280">通知</span><span class="sxs-lookup"><span data-stu-id="5a060-280">Acknowledgment</span></span>

<span data-ttu-id="5a060-281">[@LeviBroderick](https://twitter.com/LeviBroderick)，他寫了許多 ASP.NET 的安全性程式碼，這是大部分的資訊。</span><span class="sxs-lookup"><span data-stu-id="5a060-281">[@LeviBroderick](https://twitter.com/LeviBroderick), who wrote much of the ASP.NET security code the bulk of this information.</span></span>
